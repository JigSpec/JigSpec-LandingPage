---
id: 03-09
phase: 03
plan: 09
type: execute
wave: 3
depends_on: [03-07, 03-08]
files_modified:
  - .planning/phases/03-ship-it/03-09-SOAK-LOG.md
autonomous: false
requirements: [VISUAL-05]
tags: [soak, cold-read, lighthouse, accessibility, posthog-exception, og-debugger, preview-deploy, phase-gate]
must_haves:
  truths:
    - "PostHog `$exception` autocapture is enabled in the project Settings UI BEFORE the soak begins (Pattern 8 — USER ACTION; verify via `https://us.posthog.com/project/<id>/events?q=$exception` showing test event)"
    - "Vercel preview deploy of the post-04-03 main branch is live at a captured preview URL (Pattern 5 step 1-2)"
    - "All 16 items of the PITFALLS.md 'Looks Done But Isn't' checklist (lines 326-345 of `.planning/research/PITFALLS.md`) are individually measured and recorded in `04-04-SOAK-LOG.md` with NUMERIC values (not just ✓) — Lighthouse scores as numbers, console errors as a count, ingestion warnings as a count, cold-reader's verbatim quote"
    - "Soak window is ≥24 wall-clock hours from preview-deploy timestamp to final measurement (per ROADMAP SC#5 + RESEARCH § Pattern 5)"
    - "Console error count = 0 at T+0h, T+12h, T+24h (3 measurements)"
    - "PostHog ingestion warning count = 0 at T+0h, T+12h, T+24h (3 measurements via `https://us.posthog.com/project/<id>/data-management/ingestion-warnings`)"
    - "Lighthouse scores ≥ 95 on perf, accessibility, best-practices, AND SEO — on BOTH mobile and desktop form factors — measured via PageSpeed Insights at `https://pagespeed.web.dev/analysis?url=<preview-url>` at T+24h"
    - "Custom 404 page renders branded UI (NOT Vercel default), returns HTTP 404 status, back-link returns to `/` — verified at preview URL `<preview-url>/this-page-does-not-exist` (PITFALLS #6 + RESEARCH § Pattern 9)"
    - "OG image renders correctly in `https://www.opengraph.xyz/?url=<preview-url>` debugger — screenshot of the rendered card recorded in soak log (PITFALLS #7 + RESEARCH Pattern 5 step 6)"
    - "Mermaid chunk loads ONLY after viewport entry — verified at T+0h with browser DevTools Network tab open from page load: NO `mermaid-*.js` chunk in network log; THEN scroll to first diagram, observe chunk request fire, observe SVG render (DIAGRAM-03 production verification)"
    - "VISUAL-05 cold-read review passes (Pattern 4): ≥1 reviewer who has never seen the page describes JigSpec in their own words within 60 seconds; reviewer mentions ≥2 of (a) agentic AI / autonomous agents, (b) at least one product candidate / vertical, (c) signal-driven roadmap or runtime concept; verbatim quote recorded"
    - "Soak summary file `.planning/phases/03-ship-it/03-09-SOAK-LOG.md` exists and documents every measurement plus the cold-reader quote (per RESEARCH lines 562-564: 'must record an actual measurement for each of the 16 items, not just a ✓')"
  artifacts:
    - path: ".planning/phases/03-ship-it/03-09-SOAK-LOG.md"
      provides: "Soak measurement log — the audit trail proving Phase 4 success criteria are met"
      min_lines: 80
      contains: "T+24h"
  key_links:
    - from: "Vercel preview URL"
      to: "PostHog event ingestion"
      via: "browser → PostHog SDK → `https://us.i.posthog.com/e/` (Phase 3 wiring)"
      pattern: "soak protocol verifies the loop end-to-end with real $pageview / form-submit / diagram:view events"
    - from: "Mermaid client-render bundle"
      to: "Lighthouse mobile-perf score"
      via: "Vite chunk-split + IO viewport gating (Plan 04-01)"
      pattern: "Mermaid chunk MUST NOT appear in network log until first scroll"
    - from: "Cold-read reviewer"
      to: "VISUAL-05 acceptance"
      via: "60-second timed verbatim recall"
      pattern: "≥2 of 3 mentions = pass; otherwise file iteration sub-tasks against CONTENT-01 / CONTENT-02 / VISUAL-02 per task 6 trigger"
---

<objective>
Run the 24-hour preview soak and the cold-read review that close out Phase 4. Verify the 16-item PITFALLS.md "Looks Done But Isn't" checklist with NUMERIC measurements (not ticks), capture three time-series snapshots (T+0h / T+12h / T+24h) of console errors and PostHog ingestion warnings, run Lighthouse via PageSpeed Insights for both mobile and desktop, verify OG image rendering at opengraph.xyz, and execute the VISUAL-05 cold-read protocol with at least one external reviewer. Record every measurement in `04-04-SOAK-LOG.md`. Phase 4 cannot be declared done until all numeric thresholds are met and the cold-reader passes.

Purpose: The first three plans built and polished the page; this plan PROVES the page is shippable. Phase 4's whole identity is "preview soak" — the deliverable is not new code, it's an audit trail that justifies promoting the build to production.

Output: One new file (`04-04-SOAK-LOG.md`) with a structured table of all 16 PITFALLS measurements, three time-series snapshots, Lighthouse scores for both form factors, OG debugger screenshot reference, cold-reader verbatim quote, and a final pass/fail decision. All other "files modified" in this plan are external systems: a PostHog Settings toggle (USER ACTION) and a Vercel preview deploy (auto-deploys on push from prior plans).

Required: VISUAL-05 (cold-read review). Implicitly satisfies ROADMAP SC#5 (the 24h soak gate).

**Scope-sanity note:** Task count exceeds the 5+ heuristic; tasks 3-5 are calendar-time checkpoints (T+0h / T+12h / T+24h) during a single 24h soak window, not parallel implementation streams. Splitting would fragment a single observational protocol across multiple plans for no benefit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-diagrams-polish-preview-soak/04-RESEARCH.md
@.planning/phases/04-diagrams-polish-preview-soak/04-PATTERNS.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@vercel.json

<interfaces>
<!-- Key external systems and their access points (no in-repo code interfaces — this is a verification plan). -->

# PostHog project (already wired by Phase 1 + Phase 3)
# Project URL pattern: https://us.posthog.com/project/<id>/...
# Settings (Pattern 8): https://us.posthog.com/project/<id>/settings/autocapture
#   Toggle: "Capture exceptions" (some UI versions: "Error tracking")
# Events live tail: https://us.posthog.com/project/<id>/events
# Ingestion warnings: https://us.posthog.com/project/<id>/data-management/ingestion-warnings

# Vercel preview deploy
# Auto-deploys on push to main (Phase 1 wiring)
# URL pattern: jigspec-landing-git-main-<username>.vercel.app
#   OR per-deploy: jigspec-landing-<commit-sha>-<username>.vercel.app

# PageSpeed Insights (no auth required, free)
# https://pagespeed.web.dev/analysis?url=<preview-url>
# Returns mobile + desktop Lighthouse scores in one run
# CLI alternative if needed:
#   npx lighthouse <preview-url> --preset=mobile --output=json --output-path=./lighthouse-mobile.json
#   npx lighthouse <preview-url> --output=json --output-path=./lighthouse-desktop.json

# OpenGraph debugger (no auth required, free)
# https://www.opengraph.xyz/?url=<preview-url>
# Renders the og:image preview as it would appear in Slack/Twitter/LinkedIn

# 16-item PITFALLS checklist (verbatim from .planning/research/PITFALLS.md lines 326-345 → reproduced in RESEARCH § Pitfall 3 lines 545-562)
# Plan execution must reproduce this checklist as the soak log's TABLE OF MEASUREMENTS, with each row's "Verification" column completed.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 (CHECKPOINT): Enable PostHog $exception autocapture in project Settings</name>
  <read_first>
    - .planning/phases/04-diagrams-polish-preview-soak/04-RESEARCH.md § Pattern 8 (lines 418-431)
    - .planning/STATE.md (find PostHog project ID — locked in Phase 1)
  </read_first>
  <what-built>
    Plans 04-01 through 04-03 have built the diagrams and the polish artifacts. Plans 04-01/02 wire `track('diagram:view')` and the diagram render pipeline. Plan 04-03 ships the OG meta block, custom 404, sitemap, and static assets. The preview deploy is publishing automatically on push.
  </what-built>
  <how-to-verify>
    This step is the only one in Phase 4 that Claude literally CANNOT do — PostHog `$exception` autocapture is gated on a project Settings UI toggle (per RESEARCH Pattern 8 + Assumption A4). Without this toggle, runtime errors during soak fall on the floor and we can't measure error rate.

    User must:

    1. Open `https://us.posthog.com/project/<your-project-id>/settings/autocapture` in a browser. The project ID is the one wired in Phase 1 (locked in STATE.md).
    2. Find the toggle labeled **"Capture exceptions"** (some PostHog UI versions label it **"Error tracking"** — same toggle).
    3. Enable the toggle. Save.
    4. Verify the wiring works: open the preview URL in a browser with DevTools open. In the console, run: `throw new Error('soak-test-' + Date.now())`. Within 30 seconds, the `$exception` event should appear at `https://us.posthog.com/project/<id>/events?q=$exception` with the matching error message.
    5. Once verified, paste the test-event timestamp + the project URL into the resume signal.

    If the toggle does not exist where Pattern 8 said it would (PostHog UI may have moved it):
    - Search PostHog docs for "exception autocapture" or "error tracking".
    - Check `https://us.posthog.com/project/<id>/error_tracking` — newer PostHog versions have a dedicated section.
    - As fallback: use PostHog's "Capture all events" autocapture if exception capture is unavailable; document the deviation in the resume signal.
  </how-to-verify>
  <resume-signal>
    Type "approved" with the test-event timestamp (e.g., "approved — test event 2026-04-30T14:23:11Z appeared in events feed; toggle path was Settings > Autocapture > Capture exceptions"). If the toggle is in a different location, describe where it was found.
  </resume-signal>
  <files>(none — toggle is in PostHog project Settings UI, not the repo)</files>
  <action>See `<what-built>` + `<how-to-verify>` blocks above. The executor presents the PostHog Settings URL + the test-error-throw protocol to the user; the user enables the toggle, runs the test, and replies with the resume signal.</action>
  <verify>
    <automated>MISSING — external Settings UI toggle; verification is the human-confirmed test event. Fallback signal: a test-error timestamp pasted in the resume signal that can later be cross-referenced against PostHog events feed.</automated>
  </verify>
  <done>User confirms PostHog `$exception` autocapture is enabled and the test-error event appeared in the events feed within 30 seconds.</done>
</task>

<task type="auto">
  <name>Task 2: Capture preview URL and verify the deploy is live</name>
  <read_first>
    - vercel.json (verify `cleanUrls: true`, `trailingSlash: false`, and the CSP that Phase 3 patched)
    - .planning/phases/04-diagrams-polish-preview-soak/04-RESEARCH.md § Pattern 5 step 1-2 (preview URL capture)
  </read_first>
  <files>.planning/phases/03-ship-it/03-09-SOAK-LOG.md</files>
  <action>
    Create `.planning/phases/03-ship-it/03-09-SOAK-LOG.md` and seed it with the soak metadata. Run:

    ```bash
    # Capture the preview URL from Vercel — the assistant should query the latest deployment via the Vercel CLI if available, OR ask the user for the URL.
    # If the Vercel CLI is installed and authenticated:
    npx vercel ls --json 2>/dev/null | head -1
    # Otherwise, the user provides the URL from https://vercel.com/<team>/<project>/deployments

    # Sanity-check the URL responds:
    PREVIEW_URL="<the-captured-url>"
    curl -sI "$PREVIEW_URL" | head -5  # MUST show HTTP/2 200
    curl -s "$PREVIEW_URL" | grep -c "JigSpec" # MUST be ≥ 1

    # Capture the deploy commit SHA:
    PREVIEW_SHA=$(curl -s "$PREVIEW_URL" | grep -oE 'data-commit-sha="[a-f0-9]+"' || git rev-parse HEAD)
    ```

    Write the soak log header with this content (substituting captured values):

    ```markdown
    # Phase 4 Soak Log

    **Plan:** 04-04
    **Phase:** 04 (Diagrams, Polish & Preview Soak)
    **Soak start:** <ISO-8601 timestamp at T+0h>
    **Soak end (target):** <T+0h plus 24 hours>
    **Preview URL:** <captured-url>
    **Deploy commit SHA:** <captured-sha>
    **PostHog project:** <project-url>
    **PostHog $exception autocapture:** ENABLED at <Task 1 timestamp>

    ## Soak protocol

    Per RESEARCH § Pattern 5 + ROADMAP SC#5. All 16 PITFALLS items measured. Three time-series snapshots (T+0h / T+12h / T+24h). Lighthouse via PSI at T+24h. Cold-read review (Pattern 4) once during the window.

    ---

    ## T+0h Snapshot

    **Timestamp:** <captured>
    **Browser:** <Chrome/Safari/Firefox + version>
    **Action:** Loaded preview URL with DevTools open. Scrolled the entire page. Hovered every product card. Visited each /products/<slug> detail page. Visited <preview-url>/this-page-does-not-exist. Submitted one test form.

    | Metric | Value |
    |--------|-------|
    | Console error count | <number> |
    | PostHog ingestion warnings count | <number> (from data-management/ingestion-warnings dashboard) |
    | Mermaid chunk in network log AT page load | <yes/no — MUST be no> |
    | Mermaid chunk in network log AFTER scroll to first diagram | <yes/no — MUST be yes> |
    | Mermaid chunk filename | <e.g., `mermaid-D8a92f1c.js`> |
    | Mermaid chunk size (transfer) | <kB> |
    | LCP element (per Lighthouse trace) | <element selector> |

    [More T+0h notes / screenshots / oddities here]

    ---

    ## T+12h Snapshot

    [Same structure as T+0h]

    ---

    ## T+24h Snapshot

    [Same structure as T+0h, plus the Lighthouse + OG + cold-read sections below.]

    ### Lighthouse via PageSpeed Insights (T+24h)

    URL: `https://pagespeed.web.dev/analysis?url=<preview-url>`

    | Form factor | Performance | Accessibility | Best Practices | SEO | Pass (all ≥95)? |
    |-------------|-------------|---------------|----------------|-----|------------------|
    | Mobile | <number> | <number> | <number> | <number> | <yes/no> |
    | Desktop | <number> | <number> | <number> | <number> | <yes/no> |

    Lighthouse audit run timestamp: <ISO-8601>
    PSI run ID / shareable link: <captured from PSI URL>

    ### OG image debugger (T+24h)

    URL: `https://www.opengraph.xyz/?url=<preview-url>`
    Result: <PASS / FAIL>
    Screenshot reference: `<path-to-screenshot-file>` (or paste an inline data: URL)
    Title rendered: <captured>
    Description rendered: <captured>
    Image rendered: <yes/no, with notes on edges/cropping>

    ### Cold-read review (Pattern 4 / VISUAL-05)

    Reviewer: <first name only — full name kept private>
    Reviewer persona: <e.g., "technical founder, has read AI/agent landing pages before, has not seen JigSpec content">
    Method: Sent ONLY the preview URL. Started 60-second timer when reviewer confirmed page loaded. Asked: "Open this and tell me in one paragraph what this company does and who it's for."
    Verbatim response: <reviewer quote>
    Pass criterion (≥ 2 of 3 must appear in the quote):
    - [ ] Mentions agentic AI / autonomous agents
    - [ ] Mentions at least one product candidate / vertical
    - [ ] Mentions signal-driven roadmap OR runtime concept

    Outcome: <PASS / FAIL>

    ---

    ## 16-item PITFALLS measurement table

    [Filled out by Task 4 — each row gets a numeric measurement, not just ✓.]

    ---

    ## Final pass/fail decision

    [Filled out by Task 7.]
    ```

    Save the file. This becomes the working document for the rest of the soak.
  </action>
  <verify>
    <automated>
      F=.planning/phases/03-ship-it/03-09-SOAK-LOG.md;
      ls $F;                                                     # MUST exist
      grep -c "Preview URL:" $F;                                 # MUST equal 1
      grep -c "Deploy commit SHA:" $F;                           # MUST equal 1
      grep -c "T+0h Snapshot" $F;                                # MUST equal 1
      grep -c "T+12h Snapshot" $F;                               # MUST equal 1
      grep -c "T+24h Snapshot" $F;                               # MUST equal 1
      grep -c "Lighthouse via PageSpeed Insights" $F;            # MUST equal 1
      grep -c "Cold-read review" $F;                             # MUST equal 1
      grep -c "16-item PITFALLS measurement table" $F;           # MUST equal 1
    </automated>
  </verify>
  <acceptance_criteria>
    - Soak log file exists with all section headers in place.
    - Preview URL captured and HTTP-200 verified.
    - Deploy commit SHA captured.
    - File ready for time-series measurements (T+0/12/24) to be filled in by Tasks 3-5.
  </acceptance_criteria>
  <done>
    Soak log scaffolded; preview deploy confirmed live; ready to start the 24h measurement window.
  </done>
</task>

<task type="auto">
  <name>Task 3: T+0h measurement — fill in soak log with initial snapshot</name>
  <read_first>
    - .planning/phases/03-ship-it/03-09-SOAK-LOG.md (just created)
    - .planning/phases/04-diagrams-polish-preview-soak/04-RESEARCH.md § Pattern 5 step 4 (T+0/12/24 protocol)
  </read_first>
  <files>.planning/phases/03-ship-it/03-09-SOAK-LOG.md</files>
  <action>
    Open the preview URL in a fresh browser session (Chrome recommended for best DevTools fidelity). Open DevTools BEFORE navigating. Open the Network tab; clear it. Open the Console tab; clear it.

    Navigate to the preview URL. Watch the network log: confirm NO `mermaid-*.js` chunk loads on initial paint.

    Now scroll the entire page slowly. As the first diagram enters the viewport (rootMargin '200px 0px' from Plan 04-01 = render starts when slot is ~200px below the fold), watch for the `mermaid-*.js` chunk to fire. Note the filename and the network-tab transfer size.

    After the page has fully scrolled and both diagrams have rendered:
    - Read the console error count. Should be 0.
    - Open `https://us.posthog.com/project/<id>/data-management/ingestion-warnings` in a separate tab. Note the count of new warnings since soak-start (use the time filter to scope to "last 1 hour").

    Run a few interaction tests:
    - Click each product card link (verify they navigate to `/products/<slug>` correctly).
    - Submit one test form on a product detail page — wait for the PostHog event to appear at `https://us.posthog.com/project/<id>/events`.
    - Visit `<preview-url>/this-page-does-not-exist` — confirm the JigSpec branded 404 renders (NOT Vercel's white 404 NOT_FOUND page) and that HTTP status is 404 (verify in DevTools Network tab → click the document → Headers panel → "Status Code: 404 Not Found").
    - Tab through the entire home page with keyboard only — verify focus rings visible on every focusable element.

    Update the T+0h section of `04-04-SOAK-LOG.md` with all measurements. Replace each `<placeholder>` with the actual captured value.

    If ANY measurement fails the threshold (console errors > 0, ingestion warnings > 0, mermaid chunk loads at page load, 404 page is Vercel's default, focus rings missing) — STOP. Do not proceed to T+12h. The page is not soak-ready. Instead:
    1. Document the failure in the soak log under a new "## T+0h Blocking Issues" section.
    2. Diagnose the root cause.
    3. Decide: fix in this plan (if a 1-line fix), or open a Phase 4.5 mini-phase if larger.
    4. Re-deploy and re-start the soak from T+0h.
  </action>
  <verify>
    <automated>
      F=.planning/phases/03-ship-it/03-09-SOAK-LOG.md;
      # All T+0h placeholders should have been replaced with real values:
      grep -c "<number>" $F;                                     # SHOULD equal 0 in the T+0h section after this task (still placeholders in T+12h/T+24h)
      # The soak log records a non-zero Mermaid chunk size as evidence the chunk did load after scroll:
      grep -c "Mermaid chunk size" $F;                           # MUST equal ≥ 3 (one per snapshot)
      # The 404 status check is documented:
      grep -c "404" $F;                                          # MUST equal ≥ 5 (header + multiple references)
    </automated>
  </verify>
  <acceptance_criteria>
    - All `<placeholder>` values in the T+0h section of the soak log are replaced with real captured values.
    - Console error count is 0 (or the failure is documented and triggers a re-soak).
    - PostHog ingestion warnings count is 0 (or documented as failure).
    - Mermaid chunk filename and size are recorded.
    - Verified the chunk does NOT load on initial paint (DIAGRAM-03 production verification).
    - 404 page rendered branded UI with HTTP 404 status.
    - Tab navigation works; focus rings visible.
  </acceptance_criteria>
  <done>
    T+0h snapshot complete and recorded. Wait 12 hours, then run Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 4: T+12h measurement + complete the 16-item PITFALLS table</name>
  <read_first>
    - .planning/phases/03-ship-it/03-09-SOAK-LOG.md (T+0h section now filled)
    - .planning/research/PITFALLS.md lines 326-345 (the 16-item checklist verbatim)
    - .planning/phases/04-diagrams-polish-preview-soak/04-RESEARCH.md § Pitfall 3 (lines 541-564)
  </read_first>
  <files>.planning/phases/03-ship-it/03-09-SOAK-LOG.md</files>
  <action>
    At the T+12h mark (12 wall-clock hours after T+0h), repeat the load-and-measure protocol from Task 3 in a different browser (so two browsers across T+0h and T+12h cover at least 2 of the 5 target browsers — Chrome, Safari, Firefox, iOS Safari, Android Chrome).

    Fill in the T+12h snapshot section with the same metric set as T+0h.

    THEN — also at T+12h — author the 16-item PITFALLS measurement table inline in the soak log. Use the verbatim 16 items from RESEARCH § Pitfall 3 (lines 545-562). Each row gets a NUMERIC or VERBATIM measurement, not just a tick. Example structure:

    ```markdown
    ## 16-item PITFALLS measurement table

    | # | Item | Verification | Measurement | Pass |
    |---|------|--------------|-------------|------|
    | 1 | All 6 product cards display correctly on iOS Safari, Android Chrome, desktop Firefox/Safari/Chrome | Manual cross-browser test during soak | Tested in: Chrome 134 macOS (T+0h, OK), Safari 17.4 macOS (T+12h, OK), iOS Safari 17.4 (T+24h, OK), Android Chrome 135 (T+24h, OK), Firefox 137 (T+24h, OK). 6/6 cards rendered correctly on all 5 browsers. | yes |
    | 2 | Mermaid diagrams legible at 320/375/414/768/1024+ px | Pattern 2 + DevTools device toolbar | Tested at all 5 viewports in DevTools device toolbar (Chrome). Smallest readable label font: <Xpx> at 320px. Both diagrams scrolled horizontally at 320px (overflow-x-auto working as designed). All node labels ≥14px at all viewports. | yes |
    | 3 | All form submits actually fire `posthog.capture` (verify in PostHog Live Events) | Submit one form per card on preview; verify each in `https://us.posthog.com/project/<id>/events` | Submitted 1 form per product card. PostHog events captured: <N> form-submit events recorded with correct `productId` payload. Verified at `<events-url-with-time-filter>`. | yes |
    | 4 | Slack notification destination receives test pings | Submit form → check Slack channel within 60s | <captured timestamp + Slack message link> | yes/no |
    | 5 | Email destination receives test pings | If Zapier-to-Gmail wired in Phase 3, test the Zap | <captured> | yes/no |
    | 6 | Custom 404 page renders, returns HTTP 404, links back to `/` | Pattern 9 verification | Visited `<preview-url>/this-page-does-not-exist`. Branded UI rendered (eyebrow `404`, headline `Not found`, italic body). HTTP status `404 Not Found` per DevTools. Back-link returned to `/` with `data-link-location="404-back-home"` event captured in PostHog. | yes |
    | 7 | OG image renders correctly in opengraph.xyz debugger | Pattern 5 step 6 | <Recorded at T+24h. See OG section above.> | yes |
    | 8 | Favicon displays in browser tab on all 5 browsers | Manual check | Verified in Chrome / Safari / Firefox / iOS Safari / Android Chrome. SVG favicon used in 4 browsers; ICO fallback in older Safari. apple-touch-icon verified by adding preview to iOS home screen. | yes |
    | 9 | No console errors on any page, any state | Pattern 5 step 4 | T+0h: 0. T+12h: 0. T+24h: 0. | yes |
    | 10 | No PostHog ingestion warnings during 24h soak | Pattern 5 step 4 — visit Data Management > Ingestion warnings | T+0h: 0. T+12h: 0. T+24h: 0. (Window: <T+0h ISO> to <T+24h ISO>.) | yes |
    | 11 | All links 200 OK (no broken internal anchors, no 404s on external links) | Manual click-through; or `lychee` link checker | Manually clicked every link. <N> total links. <M> external. All 200 OK. | yes |
    | 12 | All 5 forms validated for required fields (HTML5 + posthog.capture only fires on success) | Submit empty form → verify HTML5 validation message; submit valid → verify posthog event | <N> forms tested. Empty submits: HTML5 validation message shown for required fields, NO posthog event fired. Valid submits: posthog event captured. | yes |
    | 13 | Tab navigation works through entire page, focus rings visible | Pattern 6 manual a11y check | Tabbed through home + 1 product detail. <N> focusable elements. Every element shows the focus ring (Tailwind 4 default ring-2 ring-accent or similar). | yes |
    | 14 | Screen reader announces semantic landmarks correctly | VoiceOver test on macOS | VoiceOver enabled. Rotor showed: 1 banner (`<header>` from Nav), 1 main, 1 contentinfo (`<footer>`). Each section's heading announced. Diagrams announced as "image: <caption>" via `role="img" + aria-label`. | yes |
    | 15 | Lighthouse ≥ 95 on perf/a11y/SEO/best-practices, mobile + desktop | Pattern 7 | <Recorded at T+24h. See Lighthouse section above.> | yes |
    | 16 | External cold-reader articulates JigSpec value in 60s without leading | Pattern 4 (VISUAL-05) | <Recorded at T+24h. See Cold-read section above.> | yes |
    ```

    For items 7, 15, 16 — they reference T+24h sections that get filled by Task 5 (Lighthouse), Task 6 (cold-read). Note this in the table.

    For items 4, 5 — if Slack / Zapier are NOT yet wired (Phase 3 may have shipped without them), mark as "n/a — Phase 3 deferred" rather than fail. Phase 4 should not be blocked on Phase 3 deferred items.

    If ANY of items 1, 2, 3, 6, 8, 9, 10, 11, 12, 13, 14 fails — document the failure under a new "## T+12h Blocking Issues" section and STOP the soak. Diagnose, fix, re-deploy, re-start.
  </action>
  <verify>
    <automated>
      F=.planning/phases/03-ship-it/03-09-SOAK-LOG.md;
      grep -c "T+12h Snapshot" $F;                               # MUST equal 1
      grep -c "16-item PITFALLS measurement table" $F;           # MUST equal 1
      # The table should have 16 numbered rows:
      grep -cE "^\| [0-9]+ " $F;                                 # MUST equal 16 (one per item) — note: counts the row marker only
      # Each row must have 4 pipe-separated columns of value:
      grep -c "Pass |" $F;                                       # MUST equal 1 (the header row alone — verifies table compiled)
      # No <placeholder> tokens in the T+0h or T+12h sections:
      awk '/T\+0h Snapshot/,/T\+12h Snapshot/' $F | grep -c "<number>";  # MUST equal 0
      awk '/T\+12h Snapshot/,/T\+24h Snapshot/' $F | grep -c "<number>"; # MUST equal 0
    </automated>
  </verify>
  <acceptance_criteria>
    - T+12h snapshot section filled with real measurements.
    - 16-item PITFALLS table present with 16 rows (one per item).
    - Each row has a Verification column and a numeric/verbatim Measurement column (not just ✓).
    - Items 4-5 either tested or marked "n/a — Phase 3 deferred".
    - Items 7, 15, 16 mark "Recorded at T+24h" pending Tasks 5-6.
    - All items 1-14 (except 4, 5, 7, 15, 16) have a yes/no decision.
    - Browser coverage spans 2+ of the 5 target browsers across T+0h + T+12h.
  </acceptance_criteria>
  <done>
    T+12h snapshot recorded; PITFALLS table populated for items 1-14 (except deferred-to-T+24h items); ready for the final T+24h gate at Tasks 5-6.
  </done>
</task>

<task type="auto">
  <name>Task 5: T+24h measurement + Lighthouse via PSI + OG debugger</name>
  <read_first>
    - .planning/phases/03-ship-it/03-09-SOAK-LOG.md (T+0/12h sections filled)
    - .planning/phases/04-diagrams-polish-preview-soak/04-RESEARCH.md § Pattern 7 (Lighthouse via PSI, lines 398-417)
    - .planning/phases/04-diagrams-polish-preview-soak/04-RESEARCH.md § Pattern 5 step 5-6 (Lighthouse + OG)
  </read_first>
  <files>.planning/phases/03-ship-it/03-09-SOAK-LOG.md</files>
  <action>
    At the T+24h mark (24 wall-clock hours after T+0h), run the final measurement set:

    **Step 1: T+24h browser snapshot**

    Same load-and-measure protocol as Task 3 (and same fields filled in the T+24h snapshot section of the soak log). Use a third browser if possible to maximize cross-browser coverage.

    **Step 2: Lighthouse via PageSpeed Insights**

    1. Open `https://pagespeed.web.dev/analysis?url=<preview-url>` (substituting the captured preview URL).
    2. Wait for both mobile + desktop runs to complete (~60-90 seconds each).
    3. Copy the four scores per form factor.
    4. Capture the PSI shareable URL (PSI generates a unique URL per run for citation).
    5. If any of the 8 scores (4 categories × 2 form factors) is < 95:
       - Document the score in the soak log.
       - Click into the Lighthouse trace on PSI to identify the failing diagnostic.
       - Common fixes per RESEARCH § Pattern 7 lines 412-417: verify Mermaid chunk lazy-loaded, verify `<meta name="description">` present in Base.astro, verify Hero is the LCP candidate.
       - If the fix is a 1-line edit, apply it, push, wait for new preview, re-run PSI. Document the iteration in the soak log.
       - If the fix requires the `mmdc` fallback (DIAGRAM-05), invoke `docs/diagram-fallback-recipe.md` per the recipe — this is a SCOPE EXPANSION; document explicitly that Plan 04-04 invoked the fallback and append the iteration log.

    **Step 3: OG image debugger**

    1. Open `https://www.opengraph.xyz/?url=<preview-url>` (or the equivalent at https://www.opengraph.xyz/).
    2. Wait for the debugger to fetch + render the preview card.
    3. Verify visually:
       - Title appears: should be `JigSpec` (or whatever Plan 04-03 Task 2 set as default).
       - Description appears: should be `JigSpec — agentic AI that ships. The company-level studio behind the .pipe.yaml spec and products like buggerd.`
       - Image renders: should show the editorial wordmark from Plan 04-03 Task 4.
       - No "broken image" or "URL fetch failed" warnings.
    4. Take a screenshot of the rendered card. Save it as `.planning/phases/04-diagrams-polish-preview-soak/og-debugger-screenshot.png`.
    5. Reference the screenshot path in the soak log's OG section.
    6. As secondary validation, also try: https://cards-dev.twitter.com/validator (Twitter Card Validator) and https://developers.facebook.com/tools/debug/ (Facebook Sharing Debugger). Either is acceptable as a backup.

    **Step 4: Update PITFALLS table items 7 and 15**

    Replace the placeholders in items 7 (OG debugger) and 15 (Lighthouse) with the real measurements. Mark each as PASS only if all sub-thresholds met.

    Update the T+24h section of the soak log with all snapshots, Lighthouse scores, OG screenshot reference.
  </action>
  <verify>
    <automated>
      F=.planning/phases/03-ship-it/03-09-SOAK-LOG.md;
      grep -c "T+24h Snapshot" $F;                               # MUST equal 1
      grep -c "Lighthouse via PageSpeed Insights" $F;            # MUST equal 1
      grep -c "OG image debugger" $F;                            # MUST equal 1
      # No placeholders left in T+24h Lighthouse rows:
      awk '/Lighthouse via PageSpeed Insights/,/Cold-read review/' $F | grep -c "<number>";  # MUST equal 0
      awk '/OG image debugger/,/Cold-read review/' $F | grep -c "<captured>";  # MUST equal 0
      # OG screenshot file referenced (or inline data-URL):
      grep -cE "(og-debugger-screenshot|data:image)" $F;         # MUST equal ≥ 1
    </automated>
  </verify>
  <acceptance_criteria>
    - T+24h snapshot recorded with real values.
    - Lighthouse mobile scores ALL ≥ 95 (perf, a11y, best practices, SEO).
    - Lighthouse desktop scores ALL ≥ 95.
    - PSI shareable URL captured.
    - OG debugger result is PASS (title + description + image all render).
    - OG screenshot saved and referenced.
    - PITFALLS items 7 and 15 updated with real measurements.
    - If any Lighthouse subscore < 95, the iteration log documents the diagnosis and either the in-plan fix OR the fallback invocation.
  </acceptance_criteria>
  <done>
    Lighthouse + OG verification complete; only the cold-read remains.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6 (CHECKPOINT): VISUAL-05 cold-read review with external reviewer</name>
  <read_first>
    - .planning/phases/04-diagrams-polish-preview-soak/04-RESEARCH.md § Pattern 4 (cold-read review, lines 335-348)
    - .planning/phases/03-ship-it/03-09-SOAK-LOG.md (the cold-read section is empty awaiting this task)
    - .planning/REQUIREMENTS.md (CONTENT-01, CONTENT-02, VISUAL-02 — the iteration trigger maps confusion to these specific IDs)
    - CLAUDE.md voice constraints (the cold-reader's recall is the validation that the locked voice landed)
  </read_first>
  <what-built>
    Plans 04-01 through 04-03 shipped the diagrams + polish. Tasks 1-5 of this plan have run the soak and verified Lighthouse + OG. Only the cold-read review remains — and it is the one Phase 4 success criterion that Claude literally CANNOT execute, because it requires a human reviewer who has never seen the page.
  </what-built>
  <how-to-verify>
    Run the Pattern 4 protocol (RESEARCH lines 335-348):

    1. **Identify a reviewer.** Recruit ONE person who has never seen the JigSpec landing page. Ideal personas (per CLAUDE.md "intended visitor"):
       - A technical founder or operator who would themselves be a candidate JigSpec customer.
       - An ops lead at an AI-adjacent company.
       - A researcher who does repetitive knowledge work.
       Do NOT use: a current collaborator, a friend who has heard about the company, anyone you've explained the project to in the last 90 days.

    2. **Prep the reviewer.** Send ONLY the preview URL. Do NOT explain what JigSpec is. Do NOT give context. The literal message:
       > "Quick favor — open this URL when you have 60 seconds free, then tell me in one paragraph what this company does and who it's for. No prep, no homework, just first read."

    3. **Start the timer.** When the reviewer confirms the page has loaded, start a 60-second timer. They can scroll, but they should NOT click into product detail pages or read every word — the test is "what lands in 60 seconds".

    4. **Capture the verbatim response.** Ask them to write or speak their summary. Record it word-for-word. Do NOT paraphrase.

    5. **Score against the pass criterion (RESEARCH lines 346-347).** The reviewer's quote must mention AT LEAST 2 of these 3:
       - (a) Agentic AI / autonomous agents / "AI that does things on its own"
       - (b) At least one product candidate / vertical (buggerd, the .pipe.yaml spec, or one of the concept-stage cards)
       - (c) Signal-driven roadmap ("they're trying to figure out what to build next") OR runtime concept ("they have a system that powers products")

       2 of 3 = PASS. 1 of 3 = FAIL. 0 of 3 = catastrophic FAIL.

    6. **If FAIL — iteration trigger (named requirement IDs).** STOP. Do not declare Phase 4 done.

       If the cold-read reviewer cannot articulate JigSpec's positioning within 60 seconds, file iteration sub-tasks against the specific requirement(s) the reviewer's confusion maps to: **CONTENT-01** (hero tagline + falsifiable sub-line) if confusion is "what does this do," **CONTENT-02** (250–400 word "What is agentic AI" educational section) if confusion is "why is this different," **VISUAL-02** (3-element typographic scale — display / body / micro) if confusion is "where do I look first." Phase 4 does NOT pass VISUAL-05 acceptance until the reviewer succeeds within 60s.

       Per RESEARCH § Pitfall 2 (lines 523-540) and Pattern 4 step 6: re-test with a DIFFERENT reviewer (their first impression is now contaminated). Do NOT re-test with the same reviewer. The fix may be a 12-hour copy-iteration sprint per PITFALLS line 539. Document the failure + the iteration sub-tasks (with the requirement ID each maps to) in the soak log under "## VISUAL-05 Iteration Log" — and additionally write the verbatim confusion quote into `.planning/phases/04-diagrams-polish-preview-soak/04-COLDREAD-NOTES.md` so the iteration plan persists across plan boundaries.

    7. **If PASS:** Update the soak log's "Cold-read review" section with the reviewer's persona (anonymized), the verbatim quote, and the pass-criterion scorecard. Also write the verbatim 60-second articulation into `.planning/phases/04-diagrams-polish-preview-soak/04-COLDREAD-NOTES.md` (single-purpose record of the successful articulation, separate from the soak log audit trail). Update PITFALLS table item 16. Type the resume signal.

    The reviewer does NOT need to be technical, well-known, or "qualified" — the test is whether the page lands its positioning to a cold reader who fits the target persona. Anyone fitting one of the listed personas is a valid reviewer.
  </how-to-verify>
  <resume-signal>
    Type "approved" with the verbatim reviewer quote (e.g., "approved — reviewer K (technical founder, no prior context) said: '<paste verbatim quote>'. Scored: agentic AI: yes. Product mention: buggerd. Roadmap/runtime: 'they have a runtime that ships products'. 3/3 PASS."). If FAIL, paste the failing quote, list the iteration sub-tasks filed against CONTENT-01 / CONTENT-02 / VISUAL-02, and use a different resume signal.
  </resume-signal>
  <files>
    - .planning/phases/03-ship-it/03-09-SOAK-LOG.md
    - .planning/phases/04-diagrams-polish-preview-soak/04-COLDREAD-NOTES.md
  </files>
  <action>See `<what-built>` + `<how-to-verify>` blocks above. The executor presents the cold-read protocol (recruit reviewer, send only the URL, 60-second timer, capture verbatim quote, score against the 3-criterion rubric) to the user; the user runs the review and replies with the resume signal. After approval, the executor records the verbatim quote + scorecard in `04-04-SOAK-LOG.md` under the "Cold-read review" section, writes the verbatim articulation (or the iteration sub-task list mapped to CONTENT-01 / CONTENT-02 / VISUAL-02 if FAIL) to `04-COLDREAD-NOTES.md`, and updates PITFALLS table item 16.</action>
  <verify>
    <automated>F=.planning/phases/03-ship-it/03-09-SOAK-LOG.md; grep -c "Verbatim response:" $F; grep -c "Mentions agentic AI" $F; ls .planning/phases/04-diagrams-polish-preview-soak/04-COLDREAD-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - Reviewer's verbatim quote captured in soak log.
    - ≥2 of 3 pass-criteria met (or FAIL documented with iteration plan).
    - 04-COLDREAD-NOTES.md contains reviewer's verbatim 60-second articulation OR a list of iteration sub-tasks filed against CONTENT-01 / CONTENT-02 / VISUAL-02.
    - PITFALLS table item 16 updated with PASS/FAIL outcome.
  </acceptance_criteria>
  <done>Reviewer's verbatim quote captured in soak log AND in 04-COLDREAD-NOTES.md; ≥2 of 3 pass-criteria met (or FAIL documented with iteration sub-tasks named against CONTENT-01 / CONTENT-02 / VISUAL-02).</done>
</task>

<task type="auto">
  <name>Task 7: Write final pass/fail decision and close the soak</name>
  <read_first>
    - .planning/phases/03-ship-it/03-09-SOAK-LOG.md (all sections now filled)
    - .planning/phases/04-diagrams-polish-preview-soak/04-RESEARCH.md § Pattern 5 step 8 (final summary)
    - .planning/ROADMAP.md SC#5 (the soak gate's success criteria)
  </read_first>
  <files>.planning/phases/03-ship-it/03-09-SOAK-LOG.md</files>
  <action>
    Write the final decision section at the bottom of the soak log:

    ```markdown
    ---

    ## Final pass/fail decision

    **Decision:** PASS / FAIL
    **Decision timestamp:** <ISO-8601>

    ### Phase 4 success criteria (per ROADMAP)

    | SC | Description | Status |
    |----|-------------|--------|
    | SC#1 | Both Mermaid diagrams render legibly at 320/375/414/768/1024+ px | PASS / FAIL (refs PITFALLS items #2, #6) |
    | SC#2 | Mermaid chunk loads only after IntersectionObserver trigger (DIAGRAM-03) | PASS / FAIL (refs T+0h chunk timing measurement) |
    | SC#3 | Custom 404 page renders with HTTP 404 (DIAGRAM-05 polish item) | PASS / FAIL (refs PITFALLS item #6) |
    | SC#4 | OG image + sitemap + canonical URL emitted on every page | PASS / FAIL (refs Lighthouse SEO + OG debugger) |
    | SC#5 | 24h preview soak with all 16 PITFALLS items measured + Lighthouse ≥95 + cold-read pass | PASS / FAIL (refs entire log) |
    | SC#6 (VISUAL-05) | External cold-read confirms positioning lands | PASS / FAIL (refs cold-read section) |

    ### Aggregate metrics

    | Metric | Value |
    |--------|-------|
    | Soak duration | <T+0h to T+24h, in hours> |
    | Browsers covered | <list> |
    | Total console errors across 3 snapshots | <number> |
    | Total PostHog ingestion warnings across 3 snapshots | <number> |
    | Lighthouse mobile (lowest of 4 categories) | <number> |
    | Lighthouse desktop (lowest of 4 categories) | <number> |
    | OG debugger result | PASS / FAIL |
    | Cold-read score | <X / 3> |

    ### Outstanding items deferred to v2 / future phases

    - Per CLAUDE.md, Phase 4.5 / Phase 5 candidates that surfaced during soak.
    - <list any iteration items that did NOT block the soak gate>

    ### Sign-off

    Phase 4 is <COMPLETE / BLOCKED>. <If complete: ready for `/gsd-verify-work` and then deploy to production. If blocked: describe the blocker and propose a Phase 4.5 mini-phase or in-plan fix.>
    ```

    Replace each placeholder with the captured measurements.

    Then create `.planning/phases/03-ship-it/03-09-SUMMARY.md` per the standard GSD plan-output convention. The SUMMARY references the soak log as its supporting artifact and condenses the pass/fail decision + the cold-read quote into a 1-page reference.

    Final commit: this plan modifies only the soak log (and creates the SUMMARY). No application code changes (unless an iteration was triggered in Tasks 5/6).
  </action>
  <verify>
    <automated>
      F=.planning/phases/03-ship-it/03-09-SOAK-LOG.md;
      grep -c "## Final pass/fail decision" $F;                  # MUST equal 1
      grep -c "Decision: PASS" $F;                               # MUST equal 1 if soak passed (or "Decision: FAIL" + Phase 4.5 plan)
      grep -c "Soak duration" $F;                                # MUST equal 1
      grep -c "Cold-read score" $F;                              # MUST equal 1
      # Summary file written:
      ls .planning/phases/03-ship-it/03-09-SUMMARY.md;  # MUST exist
      grep -c "VISUAL-05" .planning/phases/03-ship-it/03-09-SUMMARY.md;  # MUST equal ≥ 1
    </automated>
  </verify>
  <acceptance_criteria>
    - Final decision section completed in soak log.
    - All 6 success-criteria rows have a PASS/FAIL status.
    - Aggregate metrics table filled with real numbers.
    - SUMMARY file exists and references the soak log.
    - If decision is FAIL, the soak log documents the proposed Phase 4.5 / in-plan-iteration path.
  </acceptance_criteria>
  <done>
    Phase 4 soak protocol complete. If PASS: ready for `/gsd-verify-work` and production promote. If FAIL: ready for Phase 4.5 mini-phase or iteration loop.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External reviewer → JigSpec preview URL | The cold-reader is given a public preview URL. They have read-only access to the page. No PII or auth involved. |
| PostHog Settings UI → autocapture wiring | User clicks a toggle in PostHog's dashboard. Phase 4 has NO source-code change to autocapture; the toggle is project-level. |
| PageSpeed Insights → preview URL | PSI fetches the preview URL from Google's infrastructure. The URL is public; no auth required. |
| Vercel preview deploy → external internet | Public preview URL. No authentication; anyone with the URL can read. Acceptable per CLAUDE.md ("preview deploys are public during soak"). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | Preview URL leaks indexing-worthy URL to reviewer (their browser history; their company analytics if they share) | accept | Preview URLs are short-lived (Vercel rotates them). The canonical link in Base.astro (Plan 04-03) points to the production domain, so even if Google indexes the preview URL, it sees `<link rel="canonical" href="https://jigspec.com/...">` and consolidates. Cosmetic risk only. |
| T-04-04-02 | Tampering | Reviewer pastes the preview URL into Slack / Twitter and the site captures their PostHog identity | accept | PostHog `person_profiles: 'identified_only'` (Phase 3 lock) means anonymous events only until form submit. The reviewer's browsing during soak does not create a person profile. |
| T-04-04-03 | Repudiation | Cold-reader's quote is misrepresented in the soak log | mitigate | Capture verbatim. Reviewer's identity is anonymized to first name only (per Task 2 + Task 6 instructions). The quote is logged before the reviewer leaves the call so they could in principle review the recorded text. |
| T-04-04-04 | Information Disclosure | PostHog Settings UI exposes `<id>` in the project URL; pasting into the soak log leaks the project ID | accept | Project IDs are not secrets. The PostHog API key (the publishable one) is already in the static page bundle by design. Project IDs are referenced in PostHog docs as URL components. No risk. |
| T-04-04-05 | DoS | PageSpeed Insights rate-limits repeated runs | accept | PSI rate-limits at ~10 runs per minute per URL. Soak protocol calls for 1-2 runs; well under the limit. If iteration triggers >5 re-runs, switch to local Lighthouse via `npx lighthouse <url>` (offline alternative, RESEARCH § Validation Architecture). |
| T-04-04-06 | Tampering | An attacker fakes a `$exception` event to PostHog to invalidate the soak metric | accept | PostHog's publishable API key allows event submission from anywhere. An attacker with the URL could in principle pollute the event stream. v1 mitigation: filter the soak log query by `properties.timestamp > <T+0h ISO>` and `properties.preview_url == <captured-url>` so out-of-band events don't count. PostHog also has IP-rate-limiting on the publishable key. Acceptable for v1. |
| T-04-04-07 | Information Disclosure | Cold-reader is given a URL that includes their referrer info | mitigate | Send the preview URL via direct message (not via a URL shortener that captures click tracking). Reviewer's referrer = their messaging app, not a third-party tracker. |
| T-04-04-08 | Tampering | Reviewer's browser extensions (e.g., uBlock) block PostHog and produce false-positive "console error" measurements | mitigate | Run T+0h / T+12h / T+24h snapshots in incognito mode with extensions disabled. Document the browser config in each snapshot. |
| T-04-04-09 | Information Disclosure | PostHog `$exception` payload may contain user PII (form-field strings, file paths, email-like substrings in stack traces) | mitigate | Review the first 10 captured `$exception` events at the T+0h soak checkpoint; if any contain PII, redact via PostHog property filters BEFORE allowing the soak window to extend past T+0h. Mermaid stack traces themselves do not include user input — Mermaid 11 does not eval `.mmd` source. PII risk is bounded to PostHog's own redaction posture. |
</threat_model>

<verification>
The soak log itself IS the verification. The phase-gate criteria:

1. `04-04-SOAK-LOG.md` exists with all section headers populated (T+0h, T+12h, T+24h, Lighthouse, OG, cold-read, 16-item PITFALLS, final decision).
2. Console errors at all 3 snapshots = 0.
3. PostHog ingestion warnings at all 3 snapshots = 0.
4. Lighthouse mobile + desktop: all 4 categories ≥ 95 on both form factors.
5. OG debugger: PASS.
6. Cold-read: ≥ 2 of 3 mentions in verbatim quote.
7. 16-item PITFALLS table: every row has a numeric/verbatim measurement; all rows in {1, 2, 3, 6, 8, 9, 10, 11, 12, 13, 14} are PASS; rows 4, 5 are PASS or "n/a — Phase 3 deferred"; rows 7, 15, 16 are PASS.
8. Final decision is PASS.
9. `03-09-SUMMARY.md` written with the cold-read quote and the pass decision.
</verification>

<success_criteria>
- VISUAL-05 satisfied (cold-read review passed with verbatim quote captured).
- ROADMAP SC#5 satisfied (24h soak with all 16 PITFALLS items measured numerically + Lighthouse ≥ 95 + OG debugger PASS).
- Soak log is the audit trail — every measurement is reproducible and dated.
- Phase 4 is COMPLETE and ready for `/gsd-verify-work` (or BLOCKED with documented iteration plan).
</success_criteria>

<output>
After completion, ensure both files exist and are committed:
- `.planning/phases/03-ship-it/03-09-SOAK-LOG.md` — the audit trail.
- `.planning/phases/03-ship-it/03-09-SUMMARY.md` — the standard GSD plan summary, condensing the soak log's pass/fail decision and the cold-reader quote into a 1-page reference.

The SUMMARY references the soak log via `@.planning/phases/03-ship-it/03-09-SOAK-LOG.md` so future phase reads can deep-link into the measurements.
</output>
