---
id: 03-07
phase: 03
plan: 07
type: execute
wave: 2
depends_on: [03-06]
files_modified:
  - src/pages/index.astro
autonomous: false
requirements: [DIAGRAM-01, DIAGRAM-02, DIAGRAM-04]
tags: [diagrams, mermaid, content, composition, mobile, checkpoint]
must_haves:
  truths:
    - "src/pages/index.astro imports `MermaidDiagram` from `'../components/diagrams/MermaidDiagram.astro'` (one new import line)"
    - "Two `<MermaidDiagram>` instances render in the page composition: `diagramId=\"pipeline-run\"` between `<AgenticAIExplainer />` and `<ProductGrid />`, and `diagramId=\"ship-to-you\"` between `<ProductGrid />` and `<ProblemPitchSection />` (per RESEARCH § Component Responsibilities table line 160)"
    - "Each instance receives `source` (Mermaid source string) and `caption` (one-sentence description) props verbatim — both required, no `?` markers"
    - "DIAGRAM-01 source uses `flowchart` syntax (per RESEARCH Open Question 1 recommendation: ship flowchart first; iterate to sequenceDiagram only if cold-reader fails); review gates render as discrete `{{...}}` hexagon nodes (NOT edge labels) per DIAGRAM-01 'visible review gates' requirement"
    - "DIAGRAM-02 source uses `flowchart LR` syntax with ≤5 nodes (per RESEARCH § Phase Requirements line 49 + § Pattern 1 line 49)"
    - "DIAGRAM-01 caption ties the diagram to CONTENT-01's reliability claim (one sentence)"
    - "DIAGRAM-02 caption explains the JigSpec-ships-to-you flow (one sentence)"
    - "Mobile legibility verified by human at 320 / 375 / 414 px viewports — every node label is ≥14px and readable; horizontal scroll on the slot is acceptable, illegible nodes are NOT (DIAGRAM-04 — checkpoint task)"
    - "TECH-04 preserved — index.astro remains composition-only; the diagram source strings are content (string literals), NOT business logic (no conditionals, no derivations, no fetches)"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Home page composition with 2 lazy-loaded Mermaid diagrams placed at the canonical narrative positions"
      contains: "MermaidDiagram"
  key_links:
    - from: "src/pages/index.astro <MermaidDiagram> instances"
      to: "src/components/diagrams/MermaidDiagram.astro Props { diagramId, source, caption }"
      via: "Astro component import + JSX-style prop passing"
      pattern: "<MermaidDiagram diagramId="
    - from: "DIAGRAM-01 + DIAGRAM-02 source strings"
      to: "Mermaid 11 parser (loaded lazily by Plan 04-01's island)"
      via: "the `source` prop, which is passed to `mermaid.render(id, source)` inside the slot's render path"
      pattern: "source={"
---

<objective>
Author the two diagram source strings and place two `<MermaidDiagram>` instances in the home-page composition at the narrative positions specified by RESEARCH § Component Responsibilities (DIAGRAM-01 between the agentic-AI explainer and the product grid; DIAGRAM-02 between the product grid and the problem-pitch capture). After implementation, verify mobile legibility manually at 320 / 375 / 414 px (DIAGRAM-04 — checkpoint).

Purpose: DIAGRAM-01 ("How an agentic pipeline runs") earns the visitor's trust by visualizing the review-gate-driven reliability claim; DIAGRAM-02 ("How JigSpec ships a product to you") frames the company-level value proposition for the product grid below. RESEARCH locks the diagram syntax (`flowchart`) and the node count (DIAGRAM-02: ≤5 nodes) and provides proposed source strings (lines 609-631) that this plan ships verbatim with minor wording polish.

Output: One modified file (`src/pages/index.astro`) with two new `<MermaidDiagram>` instances and inline-string `source` props. Two diagrams render lazily on the preview deploy when the visitor scrolls past them; mobile legibility is human-verified at three viewport widths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-diagrams-polish-preview-soak/04-RESEARCH.md
@.planning/phases/04-diagrams-polish-preview-soak/04-PATTERNS.md
@.planning/phases/04-diagrams-polish-preview-soak/04-01-PLAN.md

<!-- Source strings: 04-RESEARCH.md § Code Examples lines 609-631 (proposed; this plan polishes wording but keeps shape). -->
<!-- Composition placement: 04-PATTERNS.md § index.astro section lines 140-186. -->

<interfaces>
<!-- After Plan 04-01 ships, MermaidDiagram.astro Props interface is: -->

From src/components/diagrams/MermaidDiagram.astro (Plan 04-01 contract):
```typescript
interface Props {
  diagramId: 'pipeline-run' | 'ship-to-you';
  source: string;       // Mermaid source — must be valid Mermaid 11 syntax
  caption: string;      // one-sentence figcaption
}
```
All three props are REQUIRED (no `?` markers). The `diagramId` literal type forces compile-time exhaustiveness.

From src/pages/index.astro (current state — Phase 2 composition; Plan 04-02 is additive):
- 8 imports already present (Base, Nav, Footer, Hero, AgenticAIExplainer, ProductGrid, ProblemPitchSection, getCollection)
- Composition order in `<main>`: Hero → AgenticAIExplainer → ProductGrid → ProblemPitchSection
- Plan 04-02 adds 1 new import (MermaidDiagram) and 2 new instances (between Hero/Explainer/Grid/Pitch as specified)

CONTENT-01 reliability claim (verbatim from .planning/REQUIREMENTS.md line 12, for caption alignment):
"agents that pass your existing CI before they ship a change" — DIAGRAM-01's caption MUST tie the visible review gates back to this falsifiable framing.

Mermaid 11 syntax constraints (RESEARCH § Code Examples lines 590-605):
- `flowchart TD` (top-down) and `flowchart LR` (left-right) are both valid
- Hexagon (decision-gate) shape: `Gate{{label}}`
- Stadium (start/end) shape: `Node([label])`
- Standard rectangle: `Node[label]`
- Edge with label: `A -->|label| B`
- `securityLevel: 'strict'` (locked by Plan 04-01) means HTML in node labels is sanitized; use `<br/>` only for forced line breaks
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author DIAGRAM-01 + DIAGRAM-02 source strings and add 2 `<MermaidDiagram>` instances to index.astro</name>
  <read_first>
    - .planning/phases/04-diagrams-polish-preview-soak/04-RESEARCH.md § Code Examples (lines 609-631) — proposed Mermaid source for both diagrams.
    - .planning/phases/04-diagrams-polish-preview-soak/04-RESEARCH.md § Phase Requirements (lines 47-50) — DIAGRAM-01 must show review gates as visible nodes (rhombus/decision shape, NOT edge labels); DIAGRAM-02 must be ≤5 nodes.
    - .planning/phases/04-diagrams-polish-preview-soak/04-PATTERNS.md § index.astro section (lines 140-186) — exact import path, exact placement positions.
    - src/pages/index.astro (current state — read to confirm the 8 existing imports + composition order; Plan 04-02 is additive only).
    - .planning/REQUIREMENTS.md lines 12, 40-41 — CONTENT-01 reliability claim and DIAGRAM-01/02 wording for caption alignment.
  </read_first>
  <action>
    Edit `src/pages/index.astro` (the file is currently 28 lines per src/pages/index.astro inspection):

    **Edit 1 — Add the import.** Use the Edit tool to insert `import MermaidDiagram from '../components/diagrams/MermaidDiagram.astro';` immediately after the existing `import ProblemPitchSection from '../components/sections/ProblemPitchSection.astro';` line. Place it as the LAST component import (after ProblemPitchSection) to keep imports grouped by section/component category. Result will look like:

    ```astro
    import Hero from '../components/sections/Hero.astro';
    import AgenticAIExplainer from '../components/sections/AgenticAIExplainer.astro';
    import ProductGrid from '../components/sections/ProductGrid.astro';
    import ProblemPitchSection from '../components/sections/ProblemPitchSection.astro';
    import MermaidDiagram from '../components/diagrams/MermaidDiagram.astro';
    ```

    **Edit 2 — Insert the DIAGRAM-01 instance.** Insert between the existing `<AgenticAIExplainer />` line and the `<ProductGrid products={products} />` line. The instance MUST pass `diagramId="pipeline-run"`, `source={...}` with the Mermaid 11 source string (see below), and `caption="..."` with a one-sentence tie to CONTENT-01's reliability claim. Use a multi-line backtick template literal for `source` so the diagram is human-readable in the source file.

    Use this DIAGRAM-01 source (RESEARCH lines 609-618, polished — top-down flowchart with two visible review gates as `{{...}}` hexagons; "visible review gates" requirement satisfied by Gate1 and Gate2 being discrete graph nodes, NOT edge labels):

    ```astro
        <MermaidDiagram
          diagramId="pipeline-run"
          source={`flowchart TD
      Input([Input: ticket / claim / draft]) --> Agent[Agent + Tools]
      Agent --> Gate1{{Review Gate 1<br/>spec match?}}
      Gate1 -->|fail| Agent
      Gate1 -->|pass| Step2[Sub-task]
      Step2 --> Gate2{{Review Gate 2<br/>output valid?}}
      Gate2 -->|fail| Step2
      Gate2 -->|pass| Output([Output])`}
          caption="Each step runs against a review gate before it ships — agents that pass your existing CI before they touch production."
        />
    ```

    Caption rationale: ties directly to CONTENT-01's "agents that pass your existing CI before they ship a change" framing. The phrase "review gate" appears in both the diagram and the caption — repetition is intentional reading reinforcement.

    **Edit 3 — Insert the DIAGRAM-02 instance.** Insert between `<ProductGrid products={products} />` and `<ProblemPitchSection />`. The instance MUST pass `diagramId="ship-to-you"`, `source={...}` with the ship-to-you source, and `caption="..."` describing the company-to-product flow.

    Use this DIAGRAM-02 source (RESEARCH lines 624-629, polished — left-right flowchart with 4 nodes; ≤5-node constraint satisfied with one node to spare):

    ```astro
        <MermaidDiagram
          diagramId="ship-to-you"
          source={`flowchart LR
      Spec[.pipe.yaml spec] --> Run[JigSpec runtime runs the spec]
      Run --> Review[Review gates verify output]
      Review --> Ship[Product ships to you]`}
          caption="One spec, one runtime, the same review-gate discipline behind every product on this page."
        />
    ```

    Caption rationale: positions DIAGRAM-02 as the "what JigSpec is" frame for the product grid that just rendered above it; the phrase "review-gate discipline" echoes DIAGRAM-01's caption to reinforce the visual idea.

    **Final composition order in `<main>` (after edits):**
    ```
    <Hero />
    <AgenticAIExplainer />
    <MermaidDiagram diagramId="pipeline-run" ... />     ← NEW
    <ProductGrid products={products} />
    <MermaidDiagram diagramId="ship-to-you" ... />      ← NEW
    <ProblemPitchSection />
    ```

    **TECH-04 preservation:** index.astro stays composition-only. The diagram source strings are inline content (string literals passed as props), NOT business logic — no conditionals, no derivations, no `await` calls beyond the existing `getCollection`. The PATTERNS map § index.astro section line 185 explicitly endorses inline-string source for v1: "keep them inline in `index.astro` for v1 (only 2 diagrams; pulling them into a constants file is over-engineering at this scale)".

    Do NOT extract the diagram source strings into separate `src/diagrams/*.ts` files — over-engineering for v1.

    Do NOT add new imports beyond the one MermaidDiagram import — the source strings live inline.

    Do NOT change the existing `<Base>` props or the existing 4-section composition order.

    Pitfall to avoid (RESEARCH § Common Pitfalls Pitfall 1 line 503): Mermaid `securityLevel: 'strict'` (locked in Plan 04-01) sanitizes click handlers AND most inline HTML in node labels. The `<br/>` tag inside `{{Review Gate 1<br/>spec match?}}` is one of the few HTML tags Mermaid permits in strict mode for line breaks — verified by Mermaid 11 docs. If a future executor adds a `<script>` or `<a onclick=...>` to a label, strict mode strips it; soak verification will catch any unrendered text.
  </action>
  <verify>
    <automated>F=src/pages/index.astro &amp;&amp; grep -q "import MermaidDiagram from '../components/diagrams/MermaidDiagram.astro'" $F &amp;&amp; test "$(grep -c 'MermaidDiagram' $F)" -ge 3 &amp;&amp; grep -q 'diagramId="pipeline-run"' $F &amp;&amp; grep -q 'diagramId="ship-to-you"' $F &amp;&amp; grep -q 'flowchart TD' $F &amp;&amp; grep -q 'flowchart LR' $F &amp;&amp; grep -q 'Review Gate 1' $F &amp;&amp; grep -q 'Review Gate 2' $F &amp;&amp; grep -q 'JigSpec runtime runs the spec' $F &amp;&amp; grep -q 'review-gate discipline' $F &amp;&amp; grep -q 'pass your existing CI' $F &amp;&amp; npx astro check 2>&amp;1 | grep -q '0 errors' &amp;&amp; npm run build 2>&amp;1 | grep -qE 'Complete!|Build complete' &amp;&amp; grep -q 'data-diagram-id="pipeline-run"' dist/index.html &amp;&amp; grep -q 'data-diagram-id="ship-to-you"' dist/index.html &amp;&amp; ! grep -E '^[[:space:]]*const [A-Z_]+ = `flowchart' $F &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File `src/pages/index.astro` exists.
    - File contains the literal import line `import MermaidDiagram from '../components/diagrams/MermaidDiagram.astro';`.
    - File contains EXACTLY 2 `<MermaidDiagram` opening tags (3 total `MermaidDiagram` occurrences when counting the import line: `test "$(grep -c 'MermaidDiagram' src/pages/index.astro)" -eq 3`).
    - File contains literal `diagramId="pipeline-run"` AND `diagramId="ship-to-you"`.
    - File contains literal `flowchart TD` (DIAGRAM-01 syntax) AND `flowchart LR` (DIAGRAM-02 syntax).
    - File contains literal `Review Gate 1` AND `Review Gate 2` (DIAGRAM-01 visible review gates as discrete nodes).
    - File contains literal `JigSpec runtime runs the spec` (DIAGRAM-02 middle node).
    - File contains literal `pass your existing CI` (DIAGRAM-01 caption — CONTENT-01 alignment).
    - File contains literal `review-gate discipline` (DIAGRAM-02 caption — echo).
    - File does NOT contain a top-level `const SOURCE_PIPELINE = ...` or similar extracted constant (verifies inline-only authoring; over-engineering avoided): `! grep -E '^[[:space:]]*const [A-Z_]+ = \`flowchart' src/pages/index.astro`.
    - `npx astro check` reports zero errors.
    - `npm run build` succeeds.
    - Built `dist/index.html` contains both `data-diagram-id="pipeline-run"` AND `data-diagram-id="ship-to-you"` (proving the wrapper attributes from Plan 04-01 propagate through the build).
    - Composition order verified by ordered grep: in `src/pages/index.astro`, the line numbers must satisfy `Hero < AgenticAIExplainer < pipeline-run < ProductGrid < ship-to-you < ProblemPitchSection`. Verifiable: `awk '/<Hero \/>/{a=NR} /<AgenticAIExplainer \/>/{b=NR} /diagramId="pipeline-run"/{c=NR} /<ProductGrid/{d=NR} /diagramId="ship-to-you"/{e=NR} /<ProblemPitchSection/{f=NR} END{exit !(a<b && b<c && c<d && d<e && e<f)}' src/pages/index.astro`.
  </acceptance_criteria>
  <done>Two `<MermaidDiagram>` instances render at canonical narrative positions in `index.astro`; the build succeeds and the wrapper data attributes are present in the static HTML; the lazy-load mechanism from Plan 04-01 will fetch Mermaid only when a visitor scrolls into either diagram.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — DIAGRAM-04 mobile legibility at 320 / 375 / 414 px</name>
  <read_first>
    - .planning/phases/04-diagrams-polish-preview-soak/04-RESEARCH.md § Pattern 2 (lines 289-311) — mobile-legibility verification protocol.
    - .planning/phases/04-diagrams-polish-preview-soak/04-RESEARCH.md § Phase Requirements line 51 — DIAGRAM-04 acceptance: "horizontal scroll on the diagram element is acceptable; illegible nodes are NOT".
    - .planning/research/PITFALLS.md item 9 (line 335) — "Mermaid mobile rendering: Often missing — verify on real mobile viewports (320px / 375px), NOT just devtools narrow window."
  </read_first>
  <what-built>
    Plan 04-01 shipped a Mermaid lazy-load island. Plan 04-02 task 1 placed two `<MermaidDiagram>` instances in `src/pages/index.astro`:
    - **DIAGRAM-01** (between explainer and product grid) — `flowchart TD` with two visible review-gate hexagons (`{{Review Gate 1}}`, `{{Review Gate 2}}`); 8 nodes total
    - **DIAGRAM-02** (between product grid and problem-pitch section) — `flowchart LR` with 4 nodes (Spec → Run → Review → Ship)

    The local dev preview (`npm run dev`) now renders both diagrams when scrolled into view. The wrapper `<div class="mermaid-slot overflow-x-auto">` enables horizontal scroll on the diagram element when the SVG is wider than the viewport. Mermaid initializes lazily; on first viewport entry of the first diagram, the Mermaid runtime loads and both diagrams render.
  </what-built>
  <how-to-verify>
    **Setup:**
    1. Start the local dev server: `npm run dev`
    2. Open the preview URL (typically `http://localhost:4321`) in Chrome or Firefox.
    3. Open DevTools (Cmd+Option+I on macOS, F12 on Windows/Linux).
    4. Toggle the Device Toolbar (Cmd+Shift+M / Ctrl+Shift+M).

    **Three viewport widths to verify (per DIAGRAM-04):**

    | # | Viewport | Profile | What to check |
    |---|----------|---------|---------------|
    | 1 | 320×568 | iPhone SE (1st gen) | Both diagrams render. Every node label is readable without zooming (≥14px). Horizontal scroll IS allowed if the diagram exceeds 320px width — the slot wrapper has `overflow-x-auto`. Illegible/clipped/overlapping text is NOT acceptable. |
    | 2 | 375×667 | iPhone SE (2nd gen) / iPhone 12 mini | Same checks. This is the most common modern small-phone width. |
    | 3 | 414×896 | iPhone Plus / Pro Max | Same checks. Diagrams should comfortably fit at this width without horizontal scroll. |

    **Verification steps for each viewport:**
    1. Resize the device toolbar to the exact pixel width.
    2. Reload the page (Cmd+R / F5) to ensure fresh measurement.
    3. Scroll to DIAGRAM-01 (between explainer and product grid). Wait for the "Loading diagram…" placeholder to swap to the rendered SVG (≤2 seconds on a fast connection).
    4. Confirm: every node label readable, hexagon "Review Gate 1" and "Review Gate 2" visible as decision-shape nodes (not as edge text), arrows visible, no text overflow outside its node, no node overlapping another node.
    5. If diagram exceeds 320px width, drag-scroll horizontally inside the diagram — the page itself should NOT horizontally scroll (only the slot does).
    6. Scroll to DIAGRAM-02 (between product grid and problem-pitch). Repeat checks. The 4-node LR layout typically fits without scroll at 375px+ but may need horizontal scroll at 320px — that is acceptable.

    **Pass criteria (ALL must hold for ALL three viewports):**
    - [ ] DIAGRAM-01 renders; all 8 nodes are visible; both `Review Gate 1` and `Review Gate 2` render as hexagon-shaped nodes (visually distinct from rectangles).
    - [ ] DIAGRAM-02 renders; all 4 nodes (.pipe.yaml spec, JigSpec runtime, Review gates, Product ships) are visible.
    - [ ] All node text is readable without browser zoom (≥14px effective font size).
    - [ ] Horizontal scroll, if needed, is contained within the diagram slot — the page body does NOT horizontally scroll.
    - [ ] No console errors related to Mermaid render.

    **Fail criteria (ANY one disqualifies):**
    - Node text is clipped or rendered ≤12px (illegible).
    - Diagram nodes overlap each other.
    - Page itself horizontally scrolls (broken slot containment).
    - Mermaid render error message "Diagram failed to load" appears in the slot.

    **If FAIL at any viewport:**
    1. Document the failing viewport and the specific failure mode.
    2. Most common fix: simplify the diagram source (fewer nodes per row in DIAGRAM-01, or shorter labels in DIAGRAM-02). Edit `src/pages/index.astro` source strings.
    3. Re-run the verification.

    **Optional: verify on real device.** PITFALLS line 335 calls out the difference between DevTools narrow-window simulation and a real iPhone Safari render. If a real device is available, open the local dev URL on it (over the same wifi: `npm run dev -- --host` exposes the dev server on the LAN) and re-verify at the device's native width.

    **Record the result:** in your reply, state per viewport: `320: PASS / FAIL [reason]`, `375: PASS / FAIL [reason]`, `414: PASS / FAIL [reason]`. Include screenshot URLs/paths if helpful.
  </how-to-verify>
  <resume-signal>Type `approved 320/375/414` if all three viewports pass. If any fail, describe the failure mode and the suggested source-string change; the executor will re-iterate task 1.</resume-signal>
  <files>(none — this task only verifies the artifacts created by Task 1)</files>
  <action>See `<what-built>` and `<how-to-verify>` blocks above. The executor presents the preview URL + verification protocol to the user; the user runs the three-viewport DevTools test and replies with the resume signal.</action>
  <verify>
    <automated>MISSING — checkpoint task; verification is human (mobile legibility cannot be reliably grep-checked). Fallback automated check: `test "$(grep -c 'overflow-x-auto' src/components/diagrams/MermaidDiagram.astro)" -ge 1` confirms the slot still has horizontal-scroll affordance after Task 1.</automated>
  </verify>
  <done>User confirms DIAGRAM-04 passes at all three viewports (320 / 375 / 414 px). If any viewport fails, the executor iterates Task 1 source strings and re-presents this checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Diagram source string ↔ Mermaid renderer | Same as Plan 04-01 — but in this plan the strings are repo-committed to `src/pages/index.astro` rather than being a generic prop. No new boundary; Plan 04-01's `securityLevel: 'strict'` mitigation applies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Severity | Mitigation Plan |
|-----------|----------|-----------|-------------|----------|-----------------|
| T-04-02-01 | Tampering | A future contributor edits a diagram source string and accidentally introduces invalid Mermaid syntax (e.g., unbalanced braces) → diagram fails to render → blank slot | mitigate | Low | Plan 04-01's render error path swaps the slot innerHTML to a graceful fallback ("Diagram failed to load. Refresh the page to retry.") with a console.error. Plan 04-04's soak verification step at T+0 visually confirms both diagrams render before accepting the deploy. The Mermaid parser fails fast with line/column info, surfaced via the `console.error([MermaidDiagram] render failed for ${diagramId})` call. |
| T-04-02-02 | Information Disclosure | Diagram source strings could be authored to include sensitive copy (internal product names, pricing, customer names) by a future contributor | accept | Low | Diagram source strings are repo-committed and reviewed in PRs. The honesty audit (`npm run honesty-audit`, shipped in Phase 2) does NOT cover diagram source — but no v1 source contains marketing-buzzword bait per CONTENT-08. Soak cold-read review (Plan 04-04) catches any inadvertent issue. |
| T-04-02-03 | Tampering | Mobile legibility regression — a future copy edit lengthens a node label, clips at 320px, ships unnoticed | mitigate | Medium | DIAGRAM-04 manual checkpoint at end of this plan establishes the baseline; soak gate item 2 ("Mermaid diagrams legible at 320/375/414/768/1024+ px") re-verifies before phase exit. No automated regression test in v1 (would require a screenshot-diff infra that PITFALLS line 335 does not require). |
</threat_model>

<verification>
After both tasks complete, run from repo root:

```bash
# 1. Composition correct
F=src/pages/index.astro
grep -q "import MermaidDiagram from '../components/diagrams/MermaidDiagram.astro'" $F || { echo "missing import"; exit 1; }
test "$(grep -c '<MermaidDiagram' $F)" -eq 2 || { echo "expected exactly 2 MermaidDiagram instances"; exit 1; }
grep -q 'diagramId="pipeline-run"' $F || { echo "missing pipeline-run instance"; exit 1; }
grep -q 'diagramId="ship-to-you"' $F || { echo "missing ship-to-you instance"; exit 1; }

# 2. Order: pipeline-run between Explainer and ProductGrid; ship-to-you between ProductGrid and ProblemPitch
awk '/<Hero \/>/{a=NR} /<AgenticAIExplainer \/>/{b=NR} /diagramId="pipeline-run"/{c=NR} /<ProductGrid/{d=NR} /diagramId="ship-to-you"/{e=NR} /<ProblemPitchSection/{f=NR} END{if (a<b && b<c && c<d && d<e && e<f) print "ORDER OK"; else {print "ORDER WRONG: ", a,b,c,d,e,f; exit 1}}' $F

# 3. DIAGRAM-01 visible review gates as discrete nodes (not just edge labels)
grep -q 'Review Gate 1{{' $F || grep -q 'Gate1{{' $F || { echo "DIAGRAM-01 missing visible Gate1 hexagon"; exit 1; }
grep -q 'Review Gate 2{{' $F || grep -q 'Gate2{{' $F || { echo "DIAGRAM-01 missing visible Gate2 hexagon"; exit 1; }

# 4. DIAGRAM-02 ≤5 nodes — count node identifiers (rough heuristic: count `[` opens in the LR source block)
NODE_COUNT_DIAG2=$(awk '/diagramId="ship-to-you"/,/\/>/' $F | grep -oE '[A-Z][a-zA-Z]*\[' | sort -u | wc -l | tr -d ' ')
test "$NODE_COUNT_DIAG2" -le 5 || { echo "DIAGRAM-02 has $NODE_COUNT_DIAG2 unique nodes; max is 5"; exit 1; }

# 5. Captions present and tied to CONTENT-01
grep -q 'pass your existing CI' $F || { echo "DIAGRAM-01 caption missing CONTENT-01 alignment"; exit 1; }
grep -q 'review-gate discipline' $F || { echo "DIAGRAM-02 caption missing review-gate echo"; exit 1; }

# 6. TS contract holds + build succeeds
npx astro check 2>&1 | grep -q '0 errors' || { echo "astro check failed"; exit 1; }
npm run build 2>&1 | grep -qE 'Complete!|Build complete' || { echo "build failed"; exit 1; }

# 7. Static HTML carries both wrapper attributes (Plan 04-01 mechanism propagates)
grep -q 'data-diagram-id="pipeline-run"' dist/index.html || { echo "pipeline-run not in dist"; exit 1; }
grep -q 'data-diagram-id="ship-to-you"' dist/index.html || { echo "ship-to-you not in dist"; exit 1; }

# 8. Mermaid chunk still emitted
ls dist/_astro/ | grep -qiE 'mermaid' || { echo "Mermaid chunk regression"; exit 1; }

# (Task 2 mobile-legibility checkpoint is human-driven — separate signal.)

echo "PLAN 04-02 PASS"
```
</verification>

<success_criteria>
- `src/pages/index.astro` imports `MermaidDiagram` and renders 2 instances at the canonical narrative positions (between Explainer/ProductGrid for DIAGRAM-01; between ProductGrid/ProblemPitch for DIAGRAM-02).
- DIAGRAM-01 source uses `flowchart TD` with two `{{Review Gate ...}}` hexagon nodes — review gates are discrete, visible, decision-shape nodes (NOT edge labels).
- DIAGRAM-02 source uses `flowchart LR` with 4 nodes (≤5 per RESEARCH § Phase Requirements line 49).
- Captions tie DIAGRAM-01 to CONTENT-01's "pass your existing CI" reliability claim and DIAGRAM-02 to the company-to-product flow with a "review-gate discipline" echo.
- TECH-04 preserved — index.astro is composition-only with inline-string source props (no extracted constants, no fetches, no derivations).
- `npm run build` succeeds; built `dist/index.html` carries both `data-diagram-id` attributes; Mermaid chunk emitted in `dist/_astro/`.
- Manual mobile legibility verified at 320 / 375 / 414 px (DIAGRAM-04 — checkpoint passed).
</success_criteria>

<output>
After completion, create `.planning/phases/03-ship-it/03-07-SUMMARY.md` recording:
- Final DIAGRAM-01 source string (verbatim).
- Final DIAGRAM-02 source string (verbatim).
- Final DIAGRAM-01 caption (verbatim).
- Final DIAGRAM-02 caption (verbatim).
- DIAGRAM-04 mobile checkpoint result per viewport: 320 = PASS/FAIL, 375 = PASS/FAIL, 414 = PASS/FAIL. Include any source-string adjustments made if a viewport failed and was iterated.
- Final composition order in index.astro `<main>` (paste the lines).
- Plan 04-04 unblocked for soak verification.
</output>
