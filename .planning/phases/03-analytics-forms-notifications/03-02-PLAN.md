---
id: 03-02
phase: 03
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/lib/analytics.ts
  - src/lib/surveys.ts
  - package.json
autonomous: true
requirements: [ANALYTICS-02, ANALYTICS-03]
tags: [analytics, posthog, typed-wrapper, taxonomy, lint-guard]
must_haves:
  truths:
    - "src/lib/analytics.ts exists and exports a string-literal `EventName` union covering all 11 events from ANALYTICS-03"
    - "src/lib/analytics.ts exports `EventProps` mapped type so per-event property contracts are compile-time-enforced"
    - "src/lib/analytics.ts exports four functions: `track`, `identify`, `captureSurveyResponse`, `captureProblemPitch`"
    - "src/lib/surveys.ts exists with `Record<ProductId, SurveyConfig>` map covering all 5 non-buggerd product IDs (placeholder UUIDs — Plan 03-04 fills them)"
    - "`npm run analytics-audit` script exists in package.json and currently PASSES (zero direct posthog.capture calls in src/ outside analytics.ts)"
    - "`astro check` passes — the typed wrapper compiles cleanly under TS strict"
    - "ZERO new npm dependencies installed (research § Standard Stack 'To Add in Phase 3: None')"
  artifacts:
    - path: "src/lib/analytics.ts"
      provides: "Typed analytics wrapper — single-source-of-truth for ANALYTICS-02 + ANALYTICS-03"
      exports: ["EventName", "EventProps", "SurveyResponseProps", "ProductId-agnostic types", "track", "identify", "captureSurveyResponse", "captureProblemPitch"]
      min_lines: 70
    - path: "src/lib/surveys.ts"
      provides: "Hardcoded survey-id map per Pattern 4 — compile-time exhaustiveness via `Record<ProductId, SurveyConfig>`"
      exports: ["ProductId", "SurveyConfig", "SURVEYS", "getSurveyConfig"]
      contains: "Record<ProductId, SurveyConfig>"
    - path: "package.json"
      provides: "`scripts.analytics-audit` entry — Pattern 3 grep guard for ANALYTICS-02"
      contains: "analytics-audit"
  key_links:
    - from: "Plan 03-03 / 03-04 component scripts"
      to: "src/lib/analytics.ts"
      via: "named imports — `import { track, identify, captureSurveyResponse, captureProblemPitch } from '../../lib/analytics'`"
      pattern: "from '\\.\\./\\.\\./lib/analytics'"
    - from: "Plan 03-04 InterestForm.astro"
      to: "src/lib/surveys.ts"
      via: "import { getSurveyConfig } — resolves productId → surveyId + question UUIDs at component render time"
      pattern: "getSurveyConfig\\("
    - from: "verification chain (this plan + Plan 03-05)"
      to: "src/lib/analytics.ts (and only this file)"
      via: "`npm run analytics-audit` grep guard — fails build if `posthog.capture(` appears outside this module"
      pattern: "posthog\\.capture\\("
---

<objective>
Ship the typed analytics wrapper that ANALYTICS-02 + ANALYTICS-03 require, with zero consumers wired and zero PostHog SDK shipped to production. This plan is **code-only** — no checkpoints, no dashboard work, no Vercel deploy. Plan 03-03 will be the first plan that ships any analytics surface to the live preview.

Purpose: ANALYTICS-02 says "direct calls to `window.posthog.capture` are forbidden" — that constraint requires (a) a single typed wrapper module to route through, and (b) a grep-based audit script that fails CI if the constraint is violated. ANALYTICS-03 lists 11 event names — that contract is encoded as a TS string-literal union with discriminated-union per-event property contracts.

Output: `src/lib/analytics.ts` (typed wrapper, ~80 lines), `src/lib/surveys.ts` (hardcoded survey-id map skeleton with `PASTE_*` placeholder UUIDs that Plan 03-04 will replace), and `package.json scripts.analytics-audit` (one-line grep guard mirroring the existing `honesty-audit` pattern).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-analytics-forms-notifications/03-RESEARCH.md
@package.json

<!-- Source of truth for code: 03-RESEARCH.md § Pattern 2 (lines 391–506) for analytics.ts; -->
<!-- § Pattern 3 (lines 508–532) for analytics-audit; § Pattern 4 (lines 534–622) for surveys.ts. -->
<!-- These three sections give copy-paste-ready code. Do NOT re-derive. -->

<interfaces>
<!-- Phase 2 already emits these data attributes the typed wrapper must accept downstream consumers for: -->

From src/components/cards/ProductCard.astro (already in repo from Phase 2):
- `data-event` is set to either `card:open` (concept cards) or `card:cta_external_click` (buggerd-only).
- `data-product-id` is set to the slug.

From src/components/forms/InterestForm.astro (Phase 2 placeholder):
- `data-form="interest"` and `data-product-id={productId}` already present.
- `disabled` attributes block submission — Plan 03-04 will remove them.

From src/components/forms/ProblemPitchForm.astro (Phase 2 placeholder):
- `data-form="problem-pitch"` already present.
- Same disabled pattern.

The typed wrapper's `EventName` union and `EventProps` lookup MUST match these data-attribute string values **verbatim** — Pitfall 4 in 03-RESEARCH.md (lines 1059–1063) covers this footgun. Plan 03-05's verification chain greps the data-event values against the EventName union.

The 5 non-buggerd `ProductId` slugs (canonical, from src/content/products/*.md):
- `scientific-paper-agent`
- `triage-router-bot`
- `recorder-extractor`
- `agentic-employees`
- `delegate`

Buggerd is intentionally absent from `ProductId` — it has no interest form (CTA is external).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/lib/analytics.ts — typed wrapper with EventName union, EventProps lookup, four exported functions</name>
  <read_first>
    - 03-RESEARCH.md § Pattern 2 (lines 391–506) — copy-paste-ready code.
    - 03-RESEARCH.md § Pattern 6 (lines 761–838) — `captureProblemPitch(problem, hasEmail)` is the fourth exported function (added at the end of Pattern 6 for DEMAND-05 free-text capture).
    - 03-RESEARCH.md § Pitfall 5 (lines 1065–1072) — defensive `if (!ph) return` is required in every function (race-condition safety).
  </read_first>
  <action>
    Create `src/lib/analytics.ts` (NEW — directory `src/lib/` does not exist yet; the Write tool will create both). Content is the canonical Pattern 2 + Pattern 6 code from 03-RESEARCH.md. Specifically:

    1. **`EventName` union** (verbatim from Pattern 2, lines 408–419) — 11 string-literal members covering the full ANALYTICS-03 taxonomy:
       `'page:home_view' | 'nav:link_click' | 'card:open' | 'card:cta_external_click' | 'form:open' | 'form:abandon' | 'form:submit' | 'problem_pitch:submit' | 'diagram:view' | 'educator:scroll_complete' | 'footer:link_click'`

    2. **`EventProps` mapped type** (verbatim from Pattern 2, lines 423–435) — discriminated by event name. Each member's prop shape is:
       - `'page:home_view'`: `{ location: 'home' | 'product-detail' }`
       - `'nav:link_click'`: `{ location: string }`
       - `'card:open'`: `{ productId: string }`
       - `'card:cta_external_click'`: `{ productId: string }`
       - `'form:open'`: `{ productId?: string; form: 'interest' | 'problem-pitch' }`
       - `'form:abandon'`: `{ productId?: string; form: 'interest' | 'problem-pitch'; field?: string }`
       - `'form:submit'`: `{ productId: string; survey_id: string }`
       - `'problem_pitch:submit'`: `{ has_email: boolean }`
       - `'diagram:view'`: `{ diagram_id: 'pipeline-run' | 'ship-to-you' }`
       - `'educator:scroll_complete'`: `Record<string, never>` (no props)
       - `'footer:link_click'`: `{ location: string }`

    3. **`SurveyResponseProps` interface** (verbatim from Pattern 2, lines 446–451) — the PostHog `survey sent` event shape with `$survey_id` + template-literal-typed `$survey_response_$question_id` keys.

    4. **`declare global { interface Window { posthog?: ... } }`** block (verbatim from Pattern 2, lines 455–466) — types `window.posthog` for the wrapper's runtime reads. The Window declaration must include `capture`, `identify`, and `__loaded?`.

    5. **Four exported functions:**
       - `track<E extends EventName>(eventName: E, props: EventProps[E]): void` (Pattern 2 lines 468–478) — generic with discriminated-lookup; defensive `if (!ph) return`; logs in dev when SDK not ready.
       - `identify(email: string, traits: { first_signup_location: string }): void` (Pattern 2 lines 482–487) — calls `ph.identify(email, { email, ...traits })`. Note: research § Pitfall 5 requires `.trim()` on email **at the call site** (in form handler), not in this wrapper.
       - `captureSurveyResponse(props: SurveyResponseProps): void` (Pattern 2 lines 492–497) — calls `ph.capture('survey sent', props as Record<string, unknown>)`. Comment: "PostHog dashboard recognizes responses by $survey_id regardless of capture source [VERIFIED: posthog.com/docs/surveys/implementing-custom-surveys]".
       - `captureProblemPitch(problem: string, hasEmail: boolean): void` (Pattern 6 lines 830–835) — calls `ph.capture('problem_pitch', { problem, has_email: hasEmail })`. Comment: "DEMAND-02 free-text capture; routes through this wrapper to keep ANALYTICS-02 audit clean."

    6. **Header comment block** at top of file with these exact lines (drives `// ALLOW`-bypass intent for the audit script):
       ```
       // src/lib/analytics.ts
       //
       // THE typed wrapper. ANALYTICS-02: direct posthog.capture calls anywhere
       // outside this file are forbidden — `npm run analytics-audit` enforces.
       //
       // Per ANALYTICS-04 (identify-before-track), the call order in form submit
       // handlers is identify() THEN track('form:submit', ...) THEN
       // captureSurveyResponse({...}) — NEVER capture-then-identify.
       //
       // Source-of-truth: 03-RESEARCH.md § Pattern 2 + § Pattern 6.
       ```

    Use `import.meta.env.DEV` (Astro/Vite-standard) for the dev-only `console.warn` paths — DO NOT add a separate dev flag.

    NO `import 'posthog-js'` anywhere in this file (research § Standard Stack — the snippet pattern means zero npm dependency on posthog-js). The wrapper reads `window.posthog` only.

    File length target: 80–100 lines (matches Pattern 2's example).

    NO `tdd="true"` test file for this task — the wrapper is pure SDK plumbing with no business logic; test coverage comes from Plan 03-03's preview canary verification (the wrapper's correctness is observable end-to-end). Per task_breakdown § TDD Detection exceptions: "glue code wiring existing tested components" applies.
  </action>
  <verify>
    <automated>npx astro check 2>&amp;1 | tee /tmp/astro-check.log &amp;&amp; grep -E '0 errors' /tmp/astro-check.log &amp;&amp; grep -c "^export" src/lib/analytics.ts | grep -qE '^[5-9]|^1[0-9]' &amp;&amp; grep -E "^export type EventName" src/lib/analytics.ts &amp;&amp; grep -E "^export function track" src/lib/analytics.ts &amp;&amp; grep -E "^export function identify" src/lib/analytics.ts &amp;&amp; grep -E "^export function captureSurveyResponse" src/lib/analytics.ts &amp;&amp; grep -E "^export function captureProblemPitch" src/lib/analytics.ts &amp;&amp; ! grep -E "^import.*posthog-js" src/lib/analytics.ts &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/analytics.ts` exists.
    - File contains `export type EventName =` followed by all 11 event-name string literals (verifiable: `grep -c "'[a-z_:]*'" src/lib/analytics.ts | head -1` returns >= 11; OR explicit grep for each name).
    - File contains all four `export function` definitions: `track`, `identify`, `captureSurveyResponse`, `captureProblemPitch`.
    - File contains `declare global` Window augmentation.
    - File contains zero `import` statements that reference `'posthog-js'` (the literal package name).
    - `npx astro check` reports zero errors when run from repo root.
    - File is between 60 and 150 lines (sanity bound).
  </acceptance_criteria>
  <done>Typed analytics wrapper shipped. Plan 03-03 + Plan 03-04 component scripts can `import { track, identify, captureSurveyResponse, captureProblemPitch } from '../../lib/analytics'`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create src/lib/surveys.ts — hardcoded ProductId-keyed survey config map (placeholder UUIDs)</name>
  <read_first>
    - 03-RESEARCH.md § Pattern 4 (lines 534–622) — copy-paste-ready code.
    - 03-RESEARCH.md § Pitfall 7 (lines 1080–1084) — `'PASTE_'` placeholder strings MUST be detectable by grep so Plan 03-04's acceptance can verify replacement.
    - src/content/products/ — confirms the 5 non-buggerd ProductId slugs (already loaded above in <interfaces>).
  </read_first>
  <action>
    Create `src/lib/surveys.ts` with the verbatim Pattern 4 code (03-RESEARCH.md lines 542–614). Specifically:

    1. **Header comment** explaining the User-Action-Item-3 contract:
       ```
       // src/lib/surveys.ts
       //
       // Hardcoded survey-id map. The user CREATES the 5 Surveys in the PostHog dashboard
       // (User-Action Item 3 in 03-RESEARCH.md) and pastes the dashboard-assigned UUIDs
       // here in Plan 03-04. Each Survey has 2 question UUIDs (email + free-text context).
       //
       // astro check enforces exhaustiveness: if a productId is missing from this map,
       // `Record<ProductId, SurveyConfig>` fails the build.
       //
       // Pitfall 7: shipping with PASTE_* placeholders breaks form submissions silently.
       // Plan 03-04's acceptance criterion: `grep -r "PASTE_" src/lib/` returns zero matches
       // before commit.
       ```

    2. **`export type ProductId`** as the literal string-literal union of the 5 non-buggerd slugs (Pattern 4 lines 553–558):
       ```ts
       export type ProductId =
         | 'scientific-paper-agent'
         | 'triage-router-bot'
         | 'recorder-extractor'
         | 'agentic-employees'
         | 'delegate';
       //   ^ buggerd is intentionally absent — buggerd has no interest form.
       ```

    3. **`export interface SurveyConfig`** (Pattern 4 lines 562–568):
       ```ts
       export interface SurveyConfig {
         surveyId: string;       // $survey_id from PostHog dashboard (Plan 03-04 fills)
         questions: {
           email: string;        // question UUID for the email field
           context: string;      // question UUID for "what would you use this for"
         };
       }
       ```

    4. **`export const SURVEYS: Record<ProductId, SurveyConfig>`** with placeholder UUIDs for all 5 slugs (Pattern 4 lines 574–610). Each entry uses `surveyId: 'PASTE_SURVEY_ID_FOR_<UPPER_SLUG>'` and `questions: { email: 'PASTE_QUESTION_ID_EMAIL', context: 'PASTE_QUESTION_ID_CONTEXT' }`. The `Record<ProductId, ...>` type ensures exhaustiveness.

    5. **`export function getSurveyConfig(productId: ProductId): SurveyConfig`** (Pattern 4 lines 612–614) — trivial map lookup. Implementation: `return SURVEYS[productId];`.

    File length target: 50–80 lines.
  </action>
  <verify>
    <automated>npx astro check 2>&amp;1 | grep -E '0 errors' &amp;&amp; grep -E "^export type ProductId" src/lib/surveys.ts &amp;&amp; grep -E "^export const SURVEYS: Record&lt;ProductId, SurveyConfig&gt;" src/lib/surveys.ts &amp;&amp; grep -E "^export function getSurveyConfig" src/lib/surveys.ts &amp;&amp; test "$(grep -c PASTE_ src/lib/surveys.ts)" -ge 15 &amp;&amp; for slug in scientific-paper-agent triage-router-bot recorder-extractor agentic-employees delegate; do grep -q "'$slug':" src/lib/surveys.ts || { echo MISSING $slug; exit 1; }; done &amp;&amp; ! grep -q "'buggerd'" src/lib/surveys.ts &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/surveys.ts` exists.
    - Contains `export type ProductId` string-literal union with all 5 non-buggerd slugs.
    - Contains `export const SURVEYS: Record<ProductId, SurveyConfig>` with all 5 slugs as keys.
    - Each entry has `surveyId` AND both `questions.email` + `questions.context` set to a `PASTE_`-prefixed placeholder string.
    - **`grep -c PASTE_ src/lib/surveys.ts` returns at least 15** (5 surveyId placeholders + 5 email + 5 context = 15 — or higher if comments mention `PASTE_*`).
    - File contains NO `'buggerd'` literal (intentional absence — Plan 03-05 verification chain re-asserts).
    - `npx astro check` reports zero errors.
  </acceptance_criteria>
  <done>Survey-id map skeleton committed; Plan 03-04 will replace PASTE_* strings with real UUIDs after the user creates the 5 Surveys in the PostHog dashboard.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add `analytics-audit` script to package.json — Pattern 3 grep guard for ANALYTICS-02</name>
  <read_first>
    - 03-RESEARCH.md § Pattern 3 (lines 508–532) — copy-paste-ready one-liner.
    - package.json (current) confirms existing `honesty-audit` script — analytics-audit mirrors that pattern.
  </read_first>
  <action>
    Edit `package.json` to add a `scripts.analytics-audit` entry. Use the Edit tool to insert immediately after the existing `honesty-audit` line (preserving JSON validity).

    The script body (Pattern 3 verbatim, lines 521 — re-cited here for the executor):
    ```json
    "analytics-audit": "if grep -rEn '(window\\.)?posthog\\.capture\\(' src/ | grep -v 'src/lib/analytics.ts' | grep -v '// ALLOW'; then echo '\\nAnalytics audit FAILED — direct posthog.capture call found outside analytics.ts. See ANALYTICS-02 + Pattern 2 in 03-RESEARCH.md.' && exit 1; else echo 'Analytics audit PASSED.'; fi"
    ```

    Notes for the executor:
    - This is a single line of JSON. The shell command uses `grep -rEn` (E for extended regex, n for line numbers).
    - The `(window\\.)?posthog\\.capture\\(` regex matches BOTH `posthog.capture(` AND `window.posthog.capture(`.
    - The `grep -v 'src/lib/analytics.ts'` exclusion is the legitimate-call-site allowlist.
    - The `grep -v '// ALLOW'` exclusion is the inline opt-out for the `Analytics.astro` snippet body (Plan 03-03 will use `// ALLOW` on the canary-fire line inside the `loaded:` callback — that line is INSIDE the SDK's own bootstrap and CAN'T route through the wrapper because the wrapper requires `window.posthog` to exist, which it doesn't until inside `loaded:`).
    - The current run of this script MUST PASS (exit 0) because src/lib/analytics.ts is the only file with `posthog.capture(` and the audit excludes it.

    Pitfall to avoid: do NOT add the script as part of `prebuild` — research § Open Question 4 explicitly recommends on-demand only for v1, mirroring the `honesty-audit` pattern.

    Also add to `package.json scripts` (sibling to `honesty-audit` and `analytics-audit`):
    - **No additional script needed.** The single `analytics-audit` line satisfies Pattern 3.

    Confirm zero new dependencies — research § Standard Stack "To Add in Phase 3: None". Do NOT run `npm install` of any kind.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts['analytics-audit']) {console.error('MISSING SCRIPT'); process.exit(1)} console.log('SCRIPT PRESENT')" &amp;&amp; npm run analytics-audit 2>&amp;1 | tail -1 | grep -q 'Analytics audit PASSED' &amp;&amp; diff <(git show HEAD:package.json | node -e 'process.stdin.on("data",d=>{const j=JSON.parse(d.toString());console.log(JSON.stringify(j.dependencies||{}))})') <(node -e 'console.log(JSON.stringify(require("./package.json").dependencies||{}))') &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` is valid JSON (`node -e "JSON.parse(require('fs').readFileSync('package.json'))"` exits 0).
    - `package.json scripts` contains a literal key `"analytics-audit"`.
    - `npm run analytics-audit` exits 0 with stdout containing the literal string `Analytics audit PASSED.`.
    - `package.json dependencies` is **byte-identical** to `git show HEAD:package.json` dependencies (zero new deps — Pitfall: research § Standard Stack lock).
    - `package.json devDependencies` likewise unchanged.
    - The literal substring `posthog-js` does NOT appear anywhere in package.json (research line 1201 explicitly notes this).
  </acceptance_criteria>
  <done>Audit script ships. Running `npm run analytics-audit` becomes part of Plan 03-04 + Plan 03-05's verification chain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/lib/analytics.ts` ↔ rest of `src/` | This file is the only authorized site for `posthog.capture(` calls. The boundary is enforced by `npm run analytics-audit`. |
| Compile-time TS contract ↔ runtime data-attributes | Phase 2's `data-event` strings in components must match the `EventName` union verbatim. Boundary is enforced by Plan 03-05 verification chain (grep cross-check), not at compile time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Severity | Mitigation Plan |
|-----------|----------|-----------|-------------|----------|-----------------|
| T-03-02-01 | Tampering | Future contributor adds `posthog.capture(` outside `analytics.ts` (e.g., a quick "let me just fire one event" inline call) | mitigate | Medium | `npm run analytics-audit` greps for the pattern; Pattern 3 grep guard fails the audit. Plan 03-05 verification chain runs the audit; Phase 4+ should consider gating `npm run build` on it (Open Question 4). |
| T-03-02-02 | Tampering | Typo in `EventName` union vs Phase 2's `data-event` strings — events silently drop | mitigate | High | Plan 03-05 verification chain runs `grep -roh 'data-event="[^"]*"' src/components/` and cross-checks every value against the EventName union (Pitfall 4). This plan's `<interfaces>` block ensures the executor uses verbatim Phase 2 strings. |
| T-03-02-03 | Information disclosure | `analytics.ts` accidentally logs PII (email, free-text) to `console.warn` in dev paths | mitigate | Low | The dev-only warn paths log `eventName` and `props` — `props` for `form:submit` includes only `productId` + `survey_id` (no email/free-text); for `problem_pitch:submit` includes only `has_email` boolean. The actual problem text and email are passed as separate args to `captureProblemPitch` and `identify` respectively, neither of which logs in dev. Verified by reading Pattern 2 + Pattern 6 wrapper signatures. |
| T-03-02-04 | Denial of service | `npm run analytics-audit` regex false-matches a comment containing `posthog.capture(` literally — fails the build | accept | Low | The `// ALLOW` opt-out covers this; if a future comment trips it, add `// ALLOW` to the comment line. Documented in Pattern 3. |
| T-03-02-05 | Repudiation | Survey-id map ships with PASTE_* placeholder strings to production — events ingest as anonymous, no audit trail of intended attribution | mitigate | High | Plan 03-04 acceptance criterion: `grep -r "PASTE_" src/lib/` returns zero matches before that plan's commit. Pitfall 7 is the canonical citation. |
| T-03-02-06 | Elevation of privilege | Direct import of `posthog-js` accidentally added (sneaks past `analytics-audit` since the audit greps for `.capture(` not for the import line) | mitigate | Medium | `package.json dependencies` byte-equality check in this plan's verify command. Plan 03-05 verification chain re-asserts. |
</threat_model>

<verification>
After all 3 tasks complete, run from repo root:

```bash
# 1. TS contract: typed wrapper compiles
npx astro check 2>&1 | grep -q '0 errors' || { echo "astro check FAILED"; exit 1; }

# 2. Wrapper exports present
grep -E "^export (type EventName|function track|function identify|function captureSurveyResponse|function captureProblemPitch)" src/lib/analytics.ts | wc -l | grep -q '^[[:space:]]*5$' || { echo "analytics.ts missing exports"; exit 1; }

# 3. Survey map exhaustive (5 slugs, 15+ PASTE_ placeholders)
test "$(grep -c PASTE_ src/lib/surveys.ts)" -ge 15 || { echo "surveys.ts missing PASTE_ placeholders"; exit 1; }

# 4. Audit script PASSES (no consumers yet)
npm run analytics-audit 2>&1 | tail -1 | grep -q 'Analytics audit PASSED' || { echo "analytics-audit failed"; exit 1; }

# 5. Zero new deps
diff <(git show HEAD:package.json | node -e 'let s="";process.stdin.on("data",d=>s+=d);process.stdin.on("end",()=>{const j=JSON.parse(s);console.log(JSON.stringify({d:j.dependencies||{},dd:j.devDependencies||{}},null,2))})') <(node -e 'const j=require("./package.json");console.log(JSON.stringify({d:j.dependencies||{},dd:j.devDependencies||{}},null,2))') || { echo "deps changed"; exit 1; }

echo "PLAN 03-02 PASS"
```
</verification>

<success_criteria>
- `src/lib/analytics.ts` and `src/lib/surveys.ts` ship — the typed-wrapper contract is in place.
- `EventName` union covers all 11 events from ANALYTICS-03; `EventProps` mapped type provides per-event property contracts.
- `SURVEYS` map covers all 5 non-buggerd ProductIds (placeholder UUIDs — Plan 03-04 fills them).
- `npm run analytics-audit` script exists in package.json and currently passes (zero direct posthog.capture calls anywhere outside analytics.ts).
- `npx astro check` passes.
- ZERO new npm dependencies installed (research § Standard Stack lock).
- ZERO production HTML changes — `dist/index.html` would still contain zero `posthog` references at this point (Plan 03-03 inverts this).
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-forms-notifications/03-02-SUMMARY.md` recording:
- File creation: `src/lib/analytics.ts` (LOC count), `src/lib/surveys.ts` (LOC count).
- Script addition: `package.json scripts.analytics-audit`.
- All 11 EventName members confirmed (paste from grep output).
- All 5 ProductId slugs confirmed.
- Plan 03-03 unblocked (the wrapper is the import target for Plan 03-03's delegated listener and Plan 03-04's form submit handlers).
</output>
