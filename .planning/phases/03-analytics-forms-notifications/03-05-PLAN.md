---
id: 03-05
phase: 03
plan: 05
type: execute
wave: 5
depends_on: [03-01, 03-02, 03-03, 03-04]
files_modified:
  - docs/demand-metric.md
  - CLAUDE.md
files_created:
  - .planning/phases/03-analytics-forms-notifications/03-VERIFICATION.md
autonomous: false
requirements: [DEMAND-03, DEMAND-05]
tags: [analytics, demand-metric, dashboard, doc-fix, phase-verification, calendar-reminder]
must_haves:
  truths:
    - "docs/demand-metric.md exists with the formula `1.0×submits + 0.3×opens + 0.1×clicks + 0.05×dwell_seconds`, the gate `5 form submits per card OR 4 weeks of traffic`, and the trigger action (vertical-pick decision) — DEMAND-03"
    - "PostHog dashboard `JigSpec Demand` exists with all 5 Insights from Pattern 8 (per-card submits, per-card CTR, per-card form-open-to-submit conversion, weighted demand-rank ordering, qualitative free-text values list) — DEMAND-05"
    - "Recurring weekly calendar reminder set as the DEMAND-04 backstop"
    - "CLAUDE.md `Technology Stack` table has a clarifying note that posthog-js loads via the CDN snippet, not npm install (research line 1201)"
    - "Phase 3 verification chain runs cleanly: every requirement (ANALYTICS-01..05, DEMAND-01..05) has a verifiable artifact"
    - "Final canary verification (User-Action Item 7 second window): full surface — including form submits — fires correctly on live preview"
    - "data-event ↔ EventName cross-check passes (Pitfall 4 — every data-event string in src/components/ appears in the EventName union)"
  artifacts:
    - path: "docs/demand-metric.md"
      provides: "DEMAND-03 — formula + gate + trigger committed in writing"
      contains: "1.0 × submits + 0.3 × opens + 0.1 × clicks + 0.05 × dwell_seconds"
      min_lines: 30
    - path: "CLAUDE.md"
      provides: "Doc-fix patch — clarifies posthog-js CDN-snippet vs npm-install distinction (research line 1201)"
      contains: "CDN snippet"
    - path: ".planning/phases/03-analytics-forms-notifications/03-VERIFICATION.md"
      provides: "Phase 3 verification chain output — every ANALYTICS-/DEMAND- requirement with artifact + evidence"
      contains: "ANALYTICS-01"
  key_links:
    - from: "docs/demand-metric.md"
      to: "PostHog dashboard SQL Insight (Pattern 8 Insight 4)"
      via: "shared formula reference"
      pattern: "1\\.0.*submits.*0\\.3.*opens"
    - from: "Weekly calendar reminder"
      to: "PostHog dashboard `JigSpec Demand` URL"
      via: "calendar entry description includes the dashboard URL"
      pattern: "calendar:weekly Check JigSpec demand dashboard"
---

<objective>
Close Phase 3. Three deliverables remain after Plans 03-01..03-04:
1. **docs/demand-metric.md** — DEMAND-03's written-formula commitment.
2. **PostHog dashboard with 5 Insights** — DEMAND-05's read-side surface.
3. **Weekly calendar reminder** — DEMAND-04's backstop.

Plus the standing close-of-phase trio:
- CLAUDE.md doc-fix (mirrors Phase 2's doc-drift pattern from 02-05).
- Full Phase 3 verification chain.
- Final canary verification (User-Action Item 7 second window — the full-surface gate including form submissions).

Purpose: shippable Phase 3. After Plan 03-05 commits, ROADMAP can mark all 10 Phase 3 requirements complete and STATE.md advances to Phase 4.

Output: Two committed files (docs/demand-metric.md, CLAUDE.md patch), one phase-verification record, three user-action confirmations (dashboard built, calendar set, final canary green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-analytics-forms-notifications/03-RESEARCH.md
@.planning/phases/03-analytics-forms-notifications/03-04-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@CLAUDE.md

<!-- Source-of-truth code: 03-RESEARCH.md § Pattern 7 (lines 885–942) for demand-metric.md; -->
<!-- § Pattern 8 (lines 944–1002) for the 5 Insights spec; § User-Action Items 5, 6, 7. -->
<!-- § Constraints / Outdated to watch (line 1201) for the CLAUDE.md doc-fix. -->
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create docs/demand-metric.md (DEMAND-03)</name>
  <read_first>03-RESEARCH.md § Pattern 7 (lines 885–942) — copy-paste-ready file shape.</read_first>
  <action>
    Create the directory and file: `docs/demand-metric.md`. The directory does NOT exist yet — Write will create both. (Research § Pattern 7 line 942 explicitly notes the directory naming overlap with the future `docs.jigspec.com` is fine — internal vs public docs.)

    File content: the verbatim Pattern 7 markdown (research lines 891–935). Specifically include:

    1. **Header** with `**Locked:**` date (today: 2026-04-29) and `**Owner:** kjs`.
    2. **What this measures** section — per-card demand for the 6 product candidates surfaced on jigspec.com.
    3. **Formula** section with the exact code-fenced formula:
       ```
       demand_score = 1.0 × submits + 0.3 × opens + 0.1 × clicks + 0.05 × dwell_seconds
       ```
       AND the per-variable definitions (submits, opens, clicks, dwell_seconds) verbatim from research lines 909–916.
    4. **Gate** section with both branches:
       - 5 form submits per card for ≥1 card, OR
       - 4 weeks of traffic since launch
       AND the "if neither has triggered by 4 weeks + 1 day, the 4-week branch fires anyway" clarifier.
    5. **Action that triggers** section — vertical-pick decision recorded in `.planning/PROJECT.md` Key Decisions table.
    6. **Notes** section with the weight-rationale paragraphs from research lines 932–934.

    Length target: 35–60 lines (matches Pattern 7's example).

    **Honesty audit:** the file's prose is internal docs, not user-facing. It MUST still pass `npm run honesty-audit` (no testimonials, no fabricated metrics). Pattern 7's text is clean — preserve verbatim.
  </action>
  <verify>
    <automated>test -f docs/demand-metric.md &amp;&amp; grep -q '1.0 × submits + 0.3 × opens + 0.1 × clicks + 0.05 × dwell_seconds' docs/demand-metric.md &amp;&amp; grep -q '5 form submits per card' docs/demand-metric.md &amp;&amp; grep -q '4 weeks' docs/demand-metric.md &amp;&amp; grep -q -i 'vertical-pick' docs/demand-metric.md &amp;&amp; npm run honesty-audit 2>&amp;1 | grep -q PASSED &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/demand-metric.md` exists.
    - Contains the literal formula string `1.0 × submits + 0.3 × opens + 0.1 × clicks + 0.05 × dwell_seconds` (with the multiplication sign × — Unicode U+00D7, matches Pattern 7 verbatim).
    - Contains both gate branches: `5 form submits per card` AND `4 weeks` of traffic.
    - Contains the trigger action wording (`vertical-pick decision` or equivalent).
    - File is 30+ lines.
    - `npm run honesty-audit` passes.
  </acceptance_criteria>
  <done>DEMAND-03 satisfied — formula committed in writing.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User builds PostHog dashboard `JigSpec Demand` with 5 Insights (User-Action Item 5; DEMAND-05)</name>
  <read_first>03-RESEARCH.md § Pattern 8 (lines 944–1002) — exact spec strings for all 5 Insights.</read_first>
  <what-built>PostHog dashboard with 5 Insights — outside the codebase, dashboard-side configuration only.</what-built>
  <how-to-verify>
    Recipe (paste verbatim into chat for user; reference research § Pattern 8 lines 948–998):

    1. **PostHog Dashboard → Dashboards → New Dashboard.** Name it `JigSpec Demand`.

    2. **Add Insight 1 — Per-card submit count:**
       - Type: Trends.
       - Series 1 event: `survey sent`. Breakdown: event property `productId`. Aggregation: total count.
       - Series 2 event: `card:cta_external_click`. Breakdown: `productId` (will always be `buggerd`).
       - Time range: All time.
       - Display: Bar chart, sorted by count desc.
       - Save as `Per-card submit count`.

    3. **Add Insight 2 — Per-card click-through rate (CTR):**
       - Easiest path: Trends with formula `card:open / page:home_view × 100` grouped by productId.
       - Step approach: Funnel — Step 1 `page:home_view` (location='home'), Step 2 `card:open` per productId.
       - Display: pie or bar.
       - Save as `Per-card CTR`.

    4. **Add Insight 3 — Per-card form-open-to-submit conversion:**
       - Type: Funnel.
       - Step 1: `form:open` (form='interest', breakdown by productId).
       - Step 2: `form:submit` (breakdown by productId).
       - Display: Funnel chart.
       - Save as `Per-card form-open to submit conversion`.

    5. **Add Insight 4 — Weighted demand-rank ordering (the headline number):**
       - Type: SQL Insight (HogQL).
       - Query (paste, adjust syntax to match current PostHog HogQL — research line 973 acknowledges syntax may evolve):
         ```sql
         SELECT
           productId,
           count(*) FILTER (WHERE event = 'survey sent' OR (event = 'card:cta_external_click' AND productId = 'buggerd')) AS submits,
           count(*) FILTER (WHERE event = 'card:open') AS opens,
           count(*) FILTER (WHERE event IN ('card:open', 'card:cta_external_click')) AS clicks,
           avg(session_duration) FILTER (WHERE pathname LIKE '/products/%') AS dwell_seconds,
           (1.0 * submits) + (0.3 * opens) + (0.1 * clicks) + (0.05 * dwell_seconds) AS demand_score
         FROM events
         WHERE timestamp > toDateTime('2026-05-01')
         GROUP BY productId
         ORDER BY demand_score DESC
         ```
       - Display: Table.
       - Save as `Weighted demand-rank ordering`.

    6. **Add Insight 5 — Qualitative free-text values list:**
       - Two parallel event tables side-by-side:
         - Table A: filter `event = 'survey sent'`. Columns: timestamp, distinct_id, productId, $survey_response_$question_id (the context UUID from the relevant Survey).
         - Table B: filter `event = 'problem_pitch'`. Columns: timestamp, distinct_id, problem.
       - Sort: timestamp desc.
       - Save as `Qualitative responses`.

    7. **Confirm dashboard renders all 5 Insights** (even if empty pre-traffic).

    8. **Paste the dashboard URL into chat** for the SUMMARY.

    Acceptance: user posts the dashboard URL + a screenshot or list confirming all 5 Insights are present.
  </how-to-verify>
  <resume-signal>User pastes the dashboard URL AND confirms all 5 Insights present</resume-signal>
  <acceptance_criteria>
    - User has shared a `https://us.posthog.com/project/.../dashboard/...` URL.
    - User has confirmed all 5 Insights exist by name match: Per-card submit count, Per-card CTR, Per-card form-open to submit conversion, Weighted demand-rank ordering, Qualitative responses.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: User sets weekly calendar reminder (User-Action Item 6; DEMAND-04 backstop)</name>
  <read_first>03-RESEARCH.md § User-Action Item 6 (lines 1449–1455).</read_first>
  <what-built>Recurring calendar entry — outside the codebase.</what-built>
  <how-to-verify>
    Recipe (paste into chat):

    1. Open user's preferred calendar app (Google Calendar / iCal / Outlook).
    2. Create a recurring event:
       - **Title:** `Check JigSpec demand dashboard`
       - **Description (paste):**
         ```
         Open the PostHog 'JigSpec Demand' dashboard:
         <DASHBOARD_URL_FROM_TASK_2>

         Decision rule (per docs/demand-metric.md):
         - If 5 form submits on any card OR 4 weeks since launch (whichever first), commit a vertical-pick decision.
         - Otherwise, scan qualitative free-text and re-rank gut-check.

         15-min slot.
         ```
       - **Recurrence:** Weekly.
       - **Duration:** 15 minutes.
       - **Reminder:** 1 day before + 1 hour before.
       - **First occurrence:** 1 week from today (i.e., 2026-05-06 if today is 2026-04-29; user adjusts to next available day).

    3. Save.

    4. **Optional but recommended:** also set a one-time "4-week mark" reminder for ~28 days from today titled `JigSpec demand 4-week gate — vertical-pick due` (DEMAND-03 second branch).

    Acceptance: user confirms calendar entry exists; provides a screenshot or describes the entry.
  </how-to-verify>
  <resume-signal>User confirms `weekly calendar reminder set` with screenshot or entry description</resume-signal>
  <acceptance_criteria>
    - User has confirmed a recurring weekly calendar entry exists with the canonical title `Check JigSpec demand dashboard` (or close equivalent).
    - Entry includes a reference to the demand dashboard URL.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Doc-fix CLAUDE.md Technology Stack — clarify posthog-js loads via CDN snippet, not npm install (research line 1201)</name>
  <read_first>
    - 03-RESEARCH.md line 1201 — exact doc-drift to fix.
    - CLAUDE.md `## Technology Stack` table (lines 24-ish) — locate the existing `posthog-js` row.
    - Phase 2 SUMMARY 02-05 — mirror the doc-drift fix pattern (a small clarifying note added rather than a rewrite).
  </read_first>
  <action>
    Edit `CLAUDE.md`. Find the `## Technology Stack` table row that starts with `| **posthog-js**` (around line 16-17 of the Technology Stack section).

    Append a clarifying note in the existing "Why Recommended" cell of that row (do NOT delete the current text — extend it). The append text:

    ```
    **Note (Phase 3 clarification):** posthog-js is NOT installed via npm in v1. The runtime is loaded via the official CDN snippet pattern (Pattern 1 in 03-RESEARCH.md — `<script is:inline>` in `Analytics.astro`), which fetches `array.js` from `us-assets.i.posthog.com`. The version listed here (`^1.371`) is the CDN-loaded version's reference; package.json contains zero posthog-js dependency. Reason: the snippet bootstraps `window.posthog` synchronously and supports queued capture before the SDK is fully loaded; an npm-installed bundle does not.
    ```

    Use the Edit tool with old_string targeting the existing posthog-js row's "Why Recommended" cell and new_string appending the clarification AFTER the existing text but BEFORE the closing `|`. Be precise about table-row boundaries — the cell must remain a valid Markdown table cell.

    Alternative: insert the note as a new paragraph immediately AFTER the Core Technologies table, prefixed with `**Note (Phase 3):** ...` — this is a safer Edit boundary that doesn't risk breaking table syntax. Prefer the alternative if the existing cell is already long.

    The edit MUST NOT change any other CLAUDE.md content. The honesty-audit and any project conventions are unchanged.
  </action>
  <verify>
    <automated>grep -q 'CDN snippet' CLAUDE.md &amp;&amp; grep -q 'array.js' CLAUDE.md &amp;&amp; grep -q 'posthog-js' CLAUDE.md &amp;&amp; ! grep -E '"posthog-js"' package.json &amp;&amp; npm run honesty-audit 2>&amp;1 | grep -q PASSED &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains the literal substring `CDN snippet` AND `array.js` (the doc-fix added these).
    - The existing posthog-js Technology Stack row is preserved (does not break the Markdown table).
    - `package.json` does NOT contain `"posthog-js"` (the doc-fix is documentation only — it does NOT install posthog-js).
    - `npm run honesty-audit` passes (no fabricated metrics introduced).
  </acceptance_criteria>
  <done>CLAUDE.md doc-drift closed — future contributors won't be misled into running `npm install posthog-js`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Phase 3 verification chain — write 03-VERIFICATION.md mapping every requirement to artifact + evidence</name>
  <read_first>
    - REQUIREMENTS.md — ANALYTICS-01..05 + DEMAND-01..05 verbatim text.
    - ROADMAP.md Phase 3 Success Criteria 1-5.
    - All four prior plan SUMMARYs (03-01-SUMMARY, 03-02-SUMMARY, 03-03-SUMMARY, 03-04-SUMMARY).
    - 03-RESEARCH.md § Pitfall 4 — data-event ↔ EventName cross-check.
  </read_first>
  <action>
    Create `.planning/phases/03-analytics-forms-notifications/03-VERIFICATION.md`. This file is the canonical phase-close record showing every requirement satisfied.

    File template (fill in evidence inline as Tasks 1-4 + earlier plans confirm):

    ```markdown
    # Phase 3 Verification — Analytics, Forms & Notifications

    **Phase closed:** 2026-04-29 (or actual completion date)
    **Plans:** 03-01, 03-02, 03-03, 03-04, 03-05 — all green.

    ## Requirements coverage

    | Req ID | Description | Plan | Artifact | Evidence |
    |--------|-------------|------|----------|----------|
    | ANALYTICS-01 | PostHog SDK installed via `<script is:inline>` with locked init config | 03-03 | `src/components/global/Analytics.astro` | grep `is:inline`, `define:vars`, all 5 init keys present; canary verified on preview (03-03-SUMMARY) |
    | ANALYTICS-02 | Typed `src/lib/analytics.ts` with string-literal union; `posthog.capture(` outside is forbidden | 03-02 + 03-03 + 03-04 | `src/lib/analytics.ts` (80+ lines); `npm run analytics-audit` PASSES across all four committed plans | grep on src/ shows zero stray `posthog.capture(` |
    | ANALYTICS-03 | Full event taxonomy (11 events) | 03-02 | `EventName` union in analytics.ts | All 11 names present; data-event ↔ EventName cross-check passes |
    | ANALYTICS-04 | identify before track on form submit | 03-04 | InterestForm + ProblemPitchForm submit handlers | Line-number assertion: identify line < track line (Pattern 5 + 6 verbatim) |
    | ANALYTICS-05 | Canary events verified on preview before forms ship | 03-03 (Task 8 hard gate) | PostHog Activity feed evidence in 03-03-SUMMARY | User-pasted activity URL + timestamp |
    | DEMAND-01 | 5 per-card forms with productId discriminator + required free-text + confirmation | 03-04 | InterestForm.astro wired; surveys.ts UUID-populated | grep "'PASTE_" returns zero; preview deploy renders all 5 forms |
    | DEMAND-02 | problem-pitch form via bare posthog.capture('problem_pitch', ...) | 03-04 | ProblemPitchForm.astro wired | grep `captureProblemPitch` matches; preview test submission ingests with `problem` property |
    | DEMAND-03 | demand-metric.md committed | 03-05 | `docs/demand-metric.md` | File exists; contains formula + gate + trigger |
    | DEMAND-04 | Notification destination + weekly calendar reminder | 03-04 + 03-05 | Slack webhook OR Zapier-to-Gmail Zap; calendar entry | 03-04-SUMMARY notification screenshot; 03-05-SUMMARY calendar screenshot |
    | DEMAND-05 | PostHog dashboard with 5 Insights | 03-05 | Dashboard URL `https://us.posthog.com/project/.../dashboard/...` | 03-05 Task 2 user-action evidence |

    ## Pitfall verification chain

    | Pitfall | Mitigation | Evidence |
    |---------|------------|----------|
    | Pitfall 2 (`PUBLIC_` prefix) | Plan 03-01 `vercel env ls` × 3 scopes; Plan 03-03 dist/index.html grep | (paste evidence from SUMMARYs) |
    | Pitfall 3 (CSP `'unsafe-inline'`) | Plan 03-03 vercel.json patch; canary success on live preview | (canary URL + console clean) |
    | Pitfall 4 (data-event ↔ EventName drift) | Cross-check below | All values match |
    | Pitfall 6 (preventDefault first-line) | Plan 03-04 grep regex acceptance | Both forms pass |
    | Pitfall 7 (PASTE_ scrub) | Plan 03-04 Task 3 acceptance | grep "'PASTE_" returns 0 |
    | Pitfall 9 (Vercel scope mismatch) | Plan 03-01 Task 2 `vercel env ls` (3 rows) | (paste evidence) |

    ## data-event ↔ EventName cross-check (Pitfall 4)

    Run from repo root:
    ```bash
    DATA_EVENTS=$(grep -roh 'data-event="[^"]*"' src/components/ | sed 's/data-event="\([^"]*\)"/\1/' | sort -u)
    for e in $DATA_EVENTS; do
      grep -q "'$e'" src/lib/analytics.ts || echo "MISSING in EventName: $e"
    done
    ```
    Result: <paste output — should be empty (no MISSING lines)>.

    ## Phase 2/3 boundary inversion

    | Phase | dist/index.html `posthog` count | Verified |
    |-------|-------------------------------|----------|
    | End of Phase 2 | 0 | 02-05-SUMMARY |
    | End of Phase 3 | ≥1 | 03-03-SUMMARY (Task 7); reverified at 03-05 close |

    ## Requirements still pending after Phase 3

    None for Phase 3. Phase 4 starts with: DIAGRAM-01..05, VISUAL-05.

    ## Forward markers

    - **Phase 4:** astro-mermaid swap MUST preserve `data-diagram-id={diagramId}` on the wrapper element so the Plan 03-03 diagram:view observer keeps working.
    - **Phase 4 polish (optional):** revisit CSP `'unsafe-inline'` for `script-src` — upgrade to CSP hashes via Astro 6 `addScriptHashes` (research § Pitfall 3 path 2).
    - **Phase 4 polish (optional):** add `npm run analytics-audit` + `npm run honesty-audit` to a `prebuild` step in vercel.json `buildCommand` if either audit catches anything in the wild (research § Open Question 4).
    ```

    Fill in actual evidence by reading the prior plan SUMMARYs at write time. Run the Pitfall 4 cross-check command and paste its output (should be empty).

    File length target: 60–100 lines.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-analytics-forms-notifications/03-VERIFICATION.md &amp;&amp; for r in ANALYTICS-01 ANALYTICS-02 ANALYTICS-03 ANALYTICS-04 ANALYTICS-05 DEMAND-01 DEMAND-02 DEMAND-03 DEMAND-04 DEMAND-05; do grep -q "$r" .planning/phases/03-analytics-forms-notifications/03-VERIFICATION.md || { echo MISSING $r; exit 1; }; done &amp;&amp; DATA_EVENTS=$(grep -roh 'data-event="[^"]*"' src/components/ | sed 's/data-event="\([^"]*\)"/\1/' | sort -u) &amp;&amp; for e in $DATA_EVENTS; do grep -q "'$e'" src/lib/analytics.ts || { echo "MISSING in EventName: $e"; exit 1; }; done &amp;&amp; npm run analytics-audit 2>&amp;1 | grep -q PASSED &amp;&amp; npm run honesty-audit 2>&amp;1 | grep -q PASSED &amp;&amp; npx astro check 2>&amp;1 | grep -q '0 errors' &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `03-VERIFICATION.md` exists.
    - File contains all 10 requirement IDs (ANALYTICS-01..05, DEMAND-01..05).
    - data-event ↔ EventName cross-check produces empty MISSING list (every Phase 2 + Phase 3 emitted data-event string is in the EventName union).
    - `npm run analytics-audit`, `npm run honesty-audit`, `npx astro check` all pass.
  </acceptance_criteria>
  <done>Phase verification record committed; checker has the evidence trail.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Final canary — full surface (User-Action Item 7 second window)</name>
  <read_first>03-RESEARCH.md § User-Action Item 7 lines 1465–1473 — second execution window includes form submissions.</read_first>
  <what-built>Plan 03-05 has shipped:
    - `docs/demand-metric.md`
    - CLAUDE.md doc-fix
    - `03-VERIFICATION.md`
    - PostHog dashboard with 5 Insights (Task 2)
    - Weekly calendar reminder (Task 3)
  </what-built>
  <how-to-verify>
    Final pre-Phase-4 user verification — confirm the FULL Phase 3 surface ships events end-to-end.

    1. Visit the live preview URL.
    2. Click each nav link (verifies nav:link_click).
    3. Click each product card (verifies card:open + card:cta_external_click).
    4. Scroll the home page top to bottom (verifies educator:scroll_complete + diagram:view if any placeholder rendered).
    5. Click each footer link (verifies footer:link_click).
    6. Open `/products/scientific-paper-agent` (any one product detail page).
       - Click into the email field (verifies form:open with form='interest').
       - Submit the InterestForm with test email + test context (verifies form:submit + survey sent + identify).
       - Confirm the confirmation `<p>` appears.
    7. Return home; submit the ProblemPitchForm with optional email + a problem (verifies problem_pitch:submit + problem_pitch + optional identify).
       - Confirm the confirmation appears.
    8. Open PostHog → Activity feed.
       - Confirm ALL events appear within 5 minutes.
       - Confirm distinct_id stitching: events from before identify() should later associate with the email-keyed person (PostHog Persons view).
    9. Open the chosen notification destination (Slack OR Gmail).
       - Confirm both test submissions arrived as messages with the relevant payload.
    10. Open the PostHog dashboard `JigSpec Demand`.
        - Confirm at least one Insight (likely Insight 5 — qualitative responses) shows the test submission.
        - Confirm the dashboard renders without errors.

    If ALL 10 checks pass: type `phase 3 green` to chat.
    If any fail: describe the specific failure and decide rollback / patch / replan path.
  </how-to-verify>
  <resume-signal>User types `phase 3 green` AND optionally pastes a screenshot of the PostHog dashboard with the test submission visible</resume-signal>
  <acceptance_criteria>
    - All 10 verification checks confirmed.
    - Final canary timestamp + dashboard URL recorded for SUMMARY.
    - No outstanding errors in DevTools console / Network tab on the live preview.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/demand-metric.md` ↔ public repo | The file is committed to a public repo (assumption from Phase 1's GitHub setup). Demand metric formula is internal but not secret. |
| CLAUDE.md doc-fix ↔ future contributor / Claude execution | The doc-fix prevents a future executor from running `npm install posthog-js` based on the stale stack-research entry. |
| PostHog dashboard configuration ↔ user's PostHog account | Dashboard configuration is dashboard-side, not in repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Severity | Mitigation Plan |
|-----------|----------|-----------|-------------|----------|-----------------|
| T-03-05-01 | Tampering | CLAUDE.md edit accidentally breaks the existing posthog-js Markdown table row | mitigate | Medium | Task 4 explicitly prefers the safer alternative (insert a new paragraph after the table, not edit inside the cell). The verify command runs `npm run honesty-audit` which validates Markdown content shape implicitly. |
| T-03-05-02 | Information disclosure | docs/demand-metric.md commits implementation details (HogQL query, weights) to public git history | accept | Low | The formula and gate are not competitive secrets; making them visible aids transparency for the JigSpec team and any future contributor. PROJECT.md key decisions reference the same formula. |
| T-03-05-03 | Denial of service | data-event ↔ EventName drift introduced by a Phase 4 component edit silently breaks analytics | mitigate | Medium | The Pitfall 4 cross-check is now in `03-VERIFICATION.md`. Phase 4's plans should add the same check to their verification chains. Forward marker logged. |
| T-03-05-04 | Repudiation | User-action items 5, 6, 7 rely on screenshot evidence — could be falsely claimed | mitigate | Medium | Task 6 final canary requires `phase 3 green` AND specific verifiable artifacts (dashboard URL, notification screenshots, PostHog Activity timestamps). The phase-close checker (post-execute checker) will read 03-VERIFICATION.md and the SUMMARYs. |
| T-03-05-05 | Tampering | Future Phase 4 edits remove `data-diagram-id` attribute on diagram wrapper, silently breaking Plan 03-03's defensive observer | mitigate | Low | Forward marker recorded in `03-VERIFICATION.md` § Forward markers. Phase 4 plan-phase will read SUMMARYs and pick up the constraint. |
</threat_model>

<verification>
After all 6 tasks complete:

```bash
cd /Users/kjs/Documents/Business/JigSpec-LandingPage

# 1. Files present
test -f docs/demand-metric.md || { echo "demand-metric.md missing"; exit 1; }
test -f .planning/phases/03-analytics-forms-notifications/03-VERIFICATION.md || { echo "03-VERIFICATION.md missing"; exit 1; }

# 2. demand-metric.md content checks
grep -q '1.0 × submits + 0.3 × opens + 0.1 × clicks + 0.05 × dwell_seconds' docs/demand-metric.md
grep -q '5 form submits per card' docs/demand-metric.md
grep -q '4 weeks' docs/demand-metric.md

# 3. CLAUDE.md doc-fix
grep -q 'CDN snippet' CLAUDE.md
grep -q 'array.js' CLAUDE.md

# 4. Phase 3 audit chain
npm run honesty-audit | grep -q PASSED || { echo "honesty-audit FAILED"; exit 1; }
npm run analytics-audit | grep -q PASSED || { echo "analytics-audit FAILED"; exit 1; }
npx astro check 2>&1 | grep -q '0 errors' || { echo "astro check FAILED"; exit 1; }

# 5. data-event ↔ EventName cross-check (Pitfall 4)
DATA_EVENTS=$(grep -roh 'data-event="[^"]*"' src/components/ | sed 's/data-event="\([^"]*\)"/\1/' | sort -u)
for e in $DATA_EVENTS; do
  grep -q "'$e'" src/lib/analytics.ts || { echo "MISSING in EventName: $e"; exit 1; }
done

# 6. PASTE_ scrub still clean (regression check)
! grep "'PASTE_" src/lib/surveys.ts || { echo "PASTE_ regressed"; exit 1; }

# 7. ZERO new npm dependencies (Phase 3 invariant)
diff <(git show HEAD~5:package.json | node -e 'let s="";process.stdin.on("data",d=>s+=d);process.stdin.on("end",()=>{const j=JSON.parse(s);console.log(JSON.stringify(j.dependencies||{}))})' 2>/dev/null || echo '{}') <(node -e 'console.log(JSON.stringify(require("./package.json").dependencies||{}))') || { echo "Dependencies changed across Phase 3 — should be ZERO new deps"; }

# 8. Build clean
rm -rf dist && npm run build > /tmp/build.log 2>&1 || { echo "build FAILED"; tail /tmp/build.log; exit 1; }
grep -q 'posthog.init' dist/index.html
grep -q 'data-form="interest"' dist/products/scientific-paper-agent/index.html

echo "PLAN 03-05 LOCAL CHECKS PASS — Tasks 2/3/6 user-action gates close the phase"
```
</verification>

<success_criteria>
- DEMAND-03 satisfied: `docs/demand-metric.md` committed.
- DEMAND-05 satisfied: PostHog dashboard `JigSpec Demand` exists with 5 Insights (User-Action Item 5).
- DEMAND-04 backstop satisfied: weekly calendar reminder set (User-Action Item 6).
- CLAUDE.md doc-drift closed: posthog-js CDN-snippet vs npm-install distinction noted.
- Phase 3 verification chain runs cleanly: `03-VERIFICATION.md` records all 10 requirements with artifacts + evidence.
- Final canary green: Task 6 confirms full Phase 3 surface (including form submissions + notification destination + dashboard rendering) on live preview.
- ZERO new npm dependencies across the entire phase.
- All audits (`honesty-audit`, `analytics-audit`, `astro check`) PASS.
- ROADMAP.md Phase 3 row can be marked complete with all 10 requirements green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-forms-notifications/03-05-SUMMARY.md` recording:
- Files created: `docs/demand-metric.md`, `03-VERIFICATION.md`.
- Files modified: `CLAUDE.md` (doc-fix paragraph appended).
- PostHog dashboard URL (from Task 2).
- Calendar reminder confirmed (from Task 3).
- Final canary: timestamp + `phase 3 green` recorded; PostHog dashboard screenshot / URL.
- Zero new npm dependencies confirmed (`diff` of HEAD~5 vs current package.json shows no dependency changes).
- Phase 3 closes; STATE.md should advance to Phase 4 (DIAGRAM-01..05 + VISUAL-05).
- Forward markers for Phase 4: preserve `data-diagram-id` on diagram wrapper; consider CSP hash upgrade for `'unsafe-inline'` removal; consider gating builds on `analytics-audit` + `honesty-audit`.
</output>
