---
name: "Always-on recorder + extractor"
tagline: "[Tagline placeholder — one-line for an always-on call/meeting recorder agent, replace in copy pass]"
stage: "Probe"
order: 4
cta:
  type: "interest"
  label: "Tell us"
headline: "[Always-on recorder + extractor — falsifiable detail-page headline, replace in copy pass]"
---

[Body placeholder — meetings produce decisions and action items that vanish into note-app voids. Sales calls produce next-step commitments nobody writes down. One-on-ones produce performance signals that don't make it back to the documents that drive review cycles. The structural fix isn't another note-taker — it's an agent that listens to what a team has already chosen to record and turns the recording into the structured artifacts the team's tools already understand.]

[The agent's loop runs end-to-end on a meeting: record (or import an existing recording), transcribe, extract the decisions / action items / open questions / owners, and push them where they belong — the issue tracker for action items, the CRM for sales-call commitments, the doc the team already maintains for decisions. The reliability claim is built from boundaries, not promises. No silent edits to the team's CRM or issue tracker — every push is reviewable in a queue with the source clip linked. Recording is opt-in per meeting, not always on by default; the "always-on" label means the agent is always available, not that the microphone is always live. An on-device transcription mode exists for teams handling privacy-sensitive material so audio never leaves the laptop.]

[Audience: small remote teams whose meetings are their only synchronous artifact, founders running customer-development calls, sales operators tracking commitments across pipelines, hiring managers running structured interview loops. This card is a Probe. If you would use this — and especially if you have a hard "no audio off-device" constraint we should design around — the form below is where you tell us, with as much specificity as you can give about which tool you'd push the extracted artifacts into.]
